# 2026-08-21：真实首披日回填——阶段一取数（R5 提案的 D1 数据底料）

> 授权：用户 2026-08-21「数据回填的工作，如果不超过 100 万格可以先做掉」。
> 提案出处：`docs/plans/2026-08-14-pit-foundation-handover-proposal.md`（G4：CSMAR
> `ann_date` 是批次日非首披日）；范围收窄依据 = wind 季度行自带 `stm_issuingdate`
> → 2025Q2+ 天然满足 D1，回填重点 = 2003~2025Q1 CSMAR 段。

## 做了什么

- `fetch_stm_issuingdate.py`：按库内实存 (ts_code, end_date) 对，逐季 wss 取
  `stm_issuingdate`（`rptType=1;unit=1`），经 wind gateway。
- 范围（2026-08-21 实测）：CSMAR 段 2003+ 季末对 **286,784 个 / 89 期**。
- 计费：≈286,784 格（码 × 1 字段）。检查点 = 每 (季度, 1000 码批) 一 CSV，
  可断点续传，失败重跑最多重复计费 1,000 格。
- 产物：`checkpoints/*.csv`（原始分批）→ 合并 `stm_issuingdate_2003_2025q1.csv`
  （ts_code, end_date, stm_issuingdate 原始值含哨兵）。日志 `fetch.log`。
- 探针（发车前，5 格）：2010-12-31 老季度可取；退市票（000562.SZ 宏源证券、
  000024.SZ 招商地产）返回真实披露日 → 全历史宇宙可覆盖。

## 显式出界（D2 未覆盖标注，不静默）

| 项 | 量 | 处置 |
|---|---|---|
| `01-01` 伪行对（非报告期） | 68,096 对 | wss 无法按 rptDate 取；本就是伪行，跟随其清理线处置 |
| 2003 前对 | 22,493 对 | 提案 §三 收窄到 2003+（B3/风格线消费窗从 2003 起） |
| 取回为空/哨兵(<2000 年) 的对 | 见下方统计 | 保留在合并 CSV 中显式标注，下游用法定截止日近似兜底 |

## 覆盖统计与体检（2026-08-21 取数收官，全程 254 秒 / 328 检查点）

- **286,784 行全取回，空 0**；哨兵(<2000 年，如 1899-12-30) 11,526 →
  **有效覆盖 275,258 / 286,784 = 96.0%**。
- 体检①合理性：披露滞后（首披日−期末）中位 39 天、p5 22 / p95 157；
  **「早于期末」的不可能值 = 0**。
- 体检②滞后>1 年的 10,381 条：**10,377（100.0%）为上市前报告期**（招股书
  历史财务，首披=招股时点，正当 PIT）；余 4 条 = 年报难产实案（600472.SH
  2003 年报 2005-03 披露）+ 603361.SH（IPO 中止案，2023-09 招股书披露）。
- 体检③方向（vs CSMAR 批次日，275,258 配对）：批次日−首披日 **中位 +79 天**、
  45.0% 晚 ≥90 天（批次日陷阱的另一面实证）；相等 23.3%。
- **⚠️ 已刻画开放项**：5.82%（16,017 对）批次日反而早于 wind 日（p5 −38 天），
  特征像 CSMAR 部分行取了业绩快报日期（快报先于正式报告 1~2 月）或 wind 为
  修订版日期——集成阶段（阶段二）须定「两源不一致时的取用规则」，勿静默取 min。
- 「超法定截止 10.83%」大头 = 上述招股书类 + 本体检的截止日近似差一天
  （+2月=08-30 vs 法定 08-31，末日扎堆披露被误标），非数据病。

## 同日相邻工作：R4 中报载入（stock_selector 侧）

`scripts.load_wind_quarterly --quarters 2026-06-30`：6,500 码 × 10 字段 =
65,000 格，UPSERT **5,406 行**（~1,470 码已披露，≈23%——08-31 披露截止前的
正常进度）。**待办：9 月初（披露季收官后）再跑一次同命令补齐**（再 ~6.5 万格）。

## 未做（阶段二，待裁决/移交）

**本阶段零 DB 写入**——取数产物只落本目录 CSV。把首披日写回
`stock_financial`（新列或等价承载）涉及：
1. Market Monitor 库 DDL → 必须先过 `market-monitor/migration/SCHEMA_CHANGES.md`
   「DDL 安全快查卡」+ Pi5 双端同步（提案 D4）；
2. reader 口径切换与下游公告（提案 D5，stock_selector 侧集成）。
这两步是提案里的周级工程主体，不在「回填先做掉」的授权内自作主张。

## 回滚

阶段一无 DB 写入，无需回滚；删除本目录产物即回到取数前状态（额度已花不可回）。
