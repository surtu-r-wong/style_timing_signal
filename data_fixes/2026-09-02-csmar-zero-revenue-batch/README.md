# 2026-09-02：CSMAR 利润表「零收入省键」批量回填（13 只票 / 43 行的处置）

用户裁决：「处理一下」（承接 `data_fixes/2026-09-02-b3-salg-000820-revenue/` 里登记的同类发现）。

## 对象

CSMAR 利润表季行中「有 `B001000000`（净利润）键、无 `B001100000`（营业总收入）键」、
end_date 在 2013-01-01～2025-03-31 的行：**43 行 / 13 只票**（000820.SZ 已在前一记录中单独处理）。
机制：CSMAR 对零值省略键，营业收入为 0 的季度整块缺失，使 revenue 的 TTM / 12 季斜率（B3 SalG 等）在缺口后 11 个季度内为 NaN。

## 取数（Wind，只读；`fetch_wind.py` → `wind_fetch.json`）

`/fetch/financial_snapshot`（wss，`rptType=1;unit=1`），字段 `tot_oper_rev, oper_rev, stm_issuingdate`。
每个目标季度取值；每只票另取一个 CSMAR 有值的最近季度做口径对照；`stm_issuingdate` 与 `stock_first_disclosure` 逐行核对。

| 核对项 | 结果 |
|---|---|
| 对照季度 Wind vs CSMAR `B001100000` | 13 / 13 逐分一致 |
| 目标行 Wind `stm_issuingdate` vs 库内首披日 | 43 / 43 一致 |
| 目标行 Wind 给出 0.0 | **11 行 / 6 只票** |
| 目标行 Wind 给出非零值 | 0 |
| 目标行 Wind 亦为空（NaN） | 32 行 / 10 只票 |

## 已回填（11 行）——与 000820 同一修法

`apply_batch.py`：只处理 Wind 明确返回 0.0 的目标行；原地补 `B001100000 = B001101000 = 0.0` + 行内标记
`_backfill_2026_09_02`；`ann_date` 不动、`updated_at` 置 now()；只写 Debian 主库；幂等。
`--dry-run` 先跑 → 正式 UPDATE 11 行（rowcount 逐行断言 = 1）→ 读取端 `translate_data(...)["revenue"] == 0.0` 11/11。
快照 `db_snapshot_*.json`（`after` 原样 + `before_reconstructed` 由补丁键反推）。

| ts_code | 回填季度（Wind = 0.0） |
|---|---|
| 000996.SZ | 2024-03-31 |
| 002473.SZ | 2021-03-31 |
| 600610.SH | 2018-12-31 |
| 688192.SH | 2022-03-31, 2022-06-30, 2022-09-30, 2022-12-31, 2023-03-31, 2023-06-30 |
| 688302.SH | 2023-12-31 |
| 900906.SH | 2018-12-31 |

## 未回填（32 行）——Wind 亦为空，不能凭缺项推断为零

这些季度 CSMAR 省键、Wind `tot_oper_rev`/`oper_rev` 同为 NaN。多为科创板未盈利生物医药股（688xxx）与
ST 中毅达 A/B 股（600610.SH / 900906.SH，同一公司）：报表上营业收入大概率确实为「—」，但两个数据源都没有给出 0 这个值，
写 0 属于推断而非取数。**保持原样，是否按「无收入即 0」统一推断由用户裁**；若裁定推断，可在 `apply_batch.py` 加一条
`--infer-zero-when-wind-null` 路径并换用不同的行内标记以区分证据级别。

| ts_code | Wind 空值季度 |
|---|---|
| 600145.SH | 2018-03-31 |
| 600421.SH | 2018-03-31 |
| 600610.SH | 2018-03-31, 2018-09-30, 2019-03-31, 2019-06-30, 2019-09-30 |
| 688091.SH | 2021-09-30 |
| 688197.SH | 2023-03-31, 2024-03-31 |
| 688266.SH | 2019-12-31, 2020-03-31, 2020-06-30, 2021-03-31 |
| 688302.SH | 2022-03-31, 2022-06-30, 2022-09-30, 2023-03-31, 2023-06-30, 2023-09-30, 2024-03-31, 2025-03-31 |
| 688382.SH | 2022-09-30, 2022-12-31, 2023-03-31 |
| 688520.SH | 2021-03-31, 2021-06-30 |
| 900906.SH | 2018-03-31, 2018-09-30, 2019-03-31, 2019-06-30, 2019-09-30 |

## 下游影响

- 不触发任何 B3 run blocker（blocker 只看最后一个 formation 的 SalG 时效）。B3 `b3-continuous-style-state` 已按 r5 干净裁决关账（`closed / stop`），
  r5 归档基于本批次之前的库状态；本批次改变 6 只票的 revenue 序列，不重开也不重跑该线。
- 其他消费 `revenue` 的因子（stock_selector 侧成长类）在这 6 只票上的 TTM/斜率链条自本批次起可算。

## 回滚

```sql
UPDATE stock_selector.stock_financial
SET data = data - 'B001100000' - 'B001101000' - '_backfill_2026_09_02', updated_at = now()
WHERE statement_type = 'income' AND data_source = 'csmar' AND data ? '_backfill_2026_09_02'
  AND ts_code <> '000820.SZ';   -- 000820.SZ 两行由前一记录单独回滚
```
