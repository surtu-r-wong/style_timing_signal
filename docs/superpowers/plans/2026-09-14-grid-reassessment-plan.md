# 现役原网格重评与执行补验（2026-09-14）

执行前冻结。用户授权重新评估与补验；只研究，不改生产参数。

1. 输入复用 20260914-fixed-candidates-r1 的已校验快照，截止 2026-09-11；不读取更新行情。先校验来源 manifest 的每份使用输入哈希。
2. equal_weight 原 40 格、momentum 原 174 格全部重算，固定四对风格配置。扫描使用未四舍五入因子，与原扫描一致。旧 carry 为可用腿均值；修复 carry 为缺腿填零后两腿等权。两种算法使用完全相同价格、信号和成本，仅 carry 不同。另跑现货 LF 无 carry 口径。
3. 原窗口：2014–2020、2021–2023、2024–2026（末日受快照限制）。保留原扫描逐窗先切片再回测的边界行为。统一丢弃无标的收益的首日。成本单边 3 bps。选择键固定为前两窗 Sharpe 最小值最大；并列按原网格顺序。末窗只报告，但已被历史研究使用，不称未触碰样本。旧三窗选择和保存旧表只作追溯诊断。
4. 报告 equal_weight 现役 20/40/5 在 40 格内、slope 现役 L20/zw120/sm0 在族内和全 174 格的名次；每族代表及 L20 邻域。不能把 214 格称为全部历史尝试。
5. 执行补验候选事先限为：两现役；修复 carry 对称口径的 EW 第一、slope 族第一；无 carry LF 口径的 slope 族第一（去重）。不按执行结果增加候选。使用四位小数信号并核验现役重建与冻结生产信号一致。
6. 实际合约账本：2015-04-16 至 2026-09-11；上一日信号/OI、下一交易日收盘成交；IM 上市前全 IC，上市后 IC/IM 等权；现货用500/1000等权指数代理；单边成本 3/10 bps。真实期货价格包含基差，不额外加估计 carry。独立两池初始各半、不划转，只比较现役组合及单独/同时替换为上述候选的有限组合。
7. 全样本及原分窗描述收益、Sharpe、回撤；替换方案相对现役两池以固定移动块 20/60 日、2000 次、种子20260914报告配对 Sharpe 差区间。区间未修正此次选择偏差，仅作敏感性，不作显著通过或更换依据。
8. 验证 carry 缺腿口径、选择规则、原网格数量与生产重建；保存代码/输入/结果哈希。现有前瞻 campaign 与生产文件保持原状。独立确认仍需未来信号及成交留痕、冻结评分与已校准推断规则；当前重评不产生前瞻观察。
