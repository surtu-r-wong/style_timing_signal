# 前瞻证据链与Sharpe差校准推进（2026-09-14）

授权：用户要求继续可推进方向。本轮完成可离线验收的证据链及独立模拟研究；不修改冻结信号协议、不下单、不安装调度，不伪造未来观察。

## 证据链

新增独立市场sidecar，绑定现有campaign哈希及新增代码。CLI使用真实时钟，当日晚间只追加现货/期货日行情、OI、共同交易日历和下一交易日合约目标；保留数据库只读快照来源，拒绝补签/重复/陈旧数据。代码或协议变化需新版本，不改旧记录。

新增评分入口：校验信号与市场证据及共同日历完整性，信号缺日沿用最后目标、保留全部收益日；市场缺日、持有合约缺价或日历冲突停止评分，不插值、不悄悄删日。模拟收益与真实成交回执严格区分，当前只能建立价格驱动的研究账本。原账本提供次日收盘、前日OI、换月成本与两池独立复利；C1/C2及成本3/10bps固定。当前协议只允许期中质量检查，评分在504交易日之前不开放收益展示；离线历史演练明确标记historical_rehearsal。

测试覆盖：过期/重复/篡改/写入中断、日历缺口、信号缺失沿用与次日延迟、价格缺口拒绝、基准同步、不能把演练写入真实campaign。旧冻结文件不改。

## 独立模拟（看模拟结果前固定）

评估504个交易日的两个配对净Sharpe差。既有移动块bootstrap固定20/60日、每组499次；两个比较Holm控制家族错误。主规则固定为每个比较取两个块长p值最大值后Holm，再以双侧5%裁决；同时报告单块敏感性，不依据结果改主规则。

五种独立DGP：iid正态、共享聚集波动且t5厚尾、AR(0.3)、慢状态均值/相关、确定性波动断点。每种400份独立数据；每份四条净收益过程具有相同边际分布、总体Sharpe相同，构成两个相关比较的联合零假设。固定共同成分，保留跨比较相关；收益直接作为净收益模拟，不冒充已建模真实策略机制。

功效：iid及厚尾聚集波动场景，在第一个比较固定加入年化Sharpe差0.2/0.4/0.6，第2比较仍为零；每格400份独立数据。报告正向拒绝功效与零效应比较误报率，不据结果更改504日协议。种子2026091402，null/alternative/bootstrap独立派生流。

诊断验收：各零假设场景家族误报率的单侧95%二项上界须<=7.5%，功效按0.4增量在两个场景均>=80%为实用目标。阈值为本次模拟验收容差，不是允许部署检验按7.5%显著性。若不满足，报告未通过/功效不足，原GO保持禁用；即使满足，也仅通过列出场景，正式观察推断仍需另行绑定冻结与业务匹配审查。

## 产物与完成标准

新增market sidecar与评分模块、Sharpe校准模块和针对性测试。保存冻结计划、源码、独立样本逐次结果、摘要与测试日志；历史演练使用已有截至09-11价格。验证原campaign仍0观察且原代码指纹不变；新sidecar初始化0记录。写研究报告并更新登记，不承诺未来运行已完成。
