# 研究审计后续执行（2026-09-14）

用户授权：按审计建议的顺序推进研究、实现与核验；生产信号、实盘映射与外部数据服务不在本次变更范围。

## 顺序与完成条件

1. 实际合约执行：冻结现役 slope20 现货 long-flat / equal_weight 期货对称、两池各半，保存输入快照、可重放日账和比较表。2015-04 起 IM 上市前期货使用 IC，IM 上市后固定 IC/IM 各半；独立报告 2022-07 后真实双腿窗。不以零 carry 伪造上市前 IM。
2. 统计诊断：复现池内排位不能验证真实误报率的反例；独立生成平稳与异方差/状态变化的无预测力样本，跑同一选优和多重校正；固定 F1 的 20 个采样起点全报，修正文档语义而不挑起点翻案。
3. 风险配置：固定方向，比较当前、半空头、目标波动三个预先指定规格，使用第一阶段真实合约价格；报告回撤、尾部损失、成本、分窗与配对区间，不自动部署。
4. 固定候选补验：沿用 08-26 归档的 CUSUM/Hamilton 和 DEMA(5/7)，不优化参数；补收益方向与成本后配对检验，已有样本只作研究证据。
5. 新输入盘点：只读核对会员持仓、ETF 申赎及资金流日内字段的可得日期/字段/PIT约束，形成继续/阻塞清单，不建设新外部采集任务。

## 第一阶段固定口径（查看新回测读数前登记）

- 截止 2026-09-11；2015-04-16 起扩展窗，2022-07-25 起双腿窗；2021-23 与 2024-截止分窗。全窗先运行，再切绩效窗，避免重置跨窗仓位。
- 信号只读取当时已有生产 CSV，T 收盘信号只在下一交易日成交。
- 两宽基仅有 53 日有效开盘价（09-14只读盘点）。长期主口径是两池 **T+1 收盘成交**，另报现货 T+1 收盘 / 期货 T+1 开盘。绝不填造指数开盘价；现货仍为指数代理，实际持仓待用户补充。
- 期货主力按上一交易日 OI 最大，显式换月，在各自同一合约上计算旧持仓损益与新持仓损益。主力同 OI 时按代码排序。禁止用当日 OI 选择当日成交合约。
- 小数合约、池内名义敞口上限 1 倍、收盘价格盯市、闲置现金利息 0。按点数持仓逐日记账，成本按实际交易名义额计入；主口径 3 bps/边，10 bps/边压力情形。不另加 carry。历史保证金/限仓/整数合约/真实冲击未建模，不称为账户已实现收益。
- 两池初始各半且不日更划转资金为实际资金池主口径；每日恢复各半只作旧报告可比的辅助。分别输出各池和组合。
- 价格缺失时明确失败，不悄悄换当日主力/删除亏损日。保存输入为可重复读事务快照及 SHA-256；新 run 目录不覆盖。

## 后续固定规格（查看新读数前登记）

- 统计：独立样本数 400，置换 499 次，seed 固定 20260914；4 个因果 MA 形态 × k={5,10,20,40}，双侧 rank IC；整组共同移位。平稳独立噪声、共同确定性波动包络、分段波动/自相关三种零预测力生成机制。主循环移位按现有 min_shift=80/max_shift=n-80；另报完整循环群（含恒等变换）对照。主检验不改历史门槛。
- 风险：当前；期货空头名义额乘 0.5；两池同乘 min(1, 0.15 / 60日已实现年化波动)，波动来自 blend 现货收益，使用成交前一日信息、满60日预热。固定阈值不扫参。两个现金池比例保持各半初始。报告 20/60 日配对块重抽样 2000 次与分年读数；已看过的年份不称干净 OOS。
- 补验：现役 equal_weight 对照；raw、DEMA5、DEMA7、CUSUM、Hamilton 五个固定规格，预筛同口径 long-flat，另报对称敏感性；优先依归档脚本原定义重建并核对历史分歧计数。五比较用 Holm 校正，20/60 日块长度，不以显著性自动改生产。

## 文件与验证

新增研究模块及有行为意义的测试；收益账本测试覆盖隔夜持仓、换月价差、提前选择主力、成本、上市前替代、缺价失败、两池不划转。修正最大回撤初始净值与审计carry错位，旧机器证据保留。
新研究产物使用 backtest/output/runs/ 独立目录、输入/输出/代码指纹、manifest status=complete。结果写 docs/plans/ 并登记，索引由 tools/research_registry.py 生成。

推进中若缺账户标的，完成可独立执行的期货部分和指数代理分析，明确实际现货收益仍未验证。若新方向缺数据，完成可核验的就绪度结论，不擅自取用账户流水或搭建外部采集。
