# Gate 0 执行记录（r3 预登记 §4，2026-08-19 起）

**预登记**：`2026-08-19-fifth-bucket-preregistration-r3.md`（已冻结）。
**运行器**：`backtest/gate0_runner.py`（0r/0a/0b）；单期核对 `backtest/gate0_smoke.py`。

## 0. 跑前单期核对（§8-④，2026-08-19，`gate0_smoke.json`）

- 1000 带：名单 1000 → 快照对齐 997；因子非空率 EP/CFP/BP/DP 97~100%、SalG/ProG 83.8%、
  dROE 97.2%；腿 成长 314 / 价值 387。模拟 vs 000852 真值重合：prev=None 84.5% /
  prev=真值 92.4%（2026-06）。
- 尾部：520 只（2026-06）/437 只（2024-06），.BJ 31/13、.HK 0；日均市值中位 27.3/22.5 亿、
  最大 43.9 亿（次新巨头被排名筛结构性堵住）；快照对齐 500，SalG/ProG 非空 76%；
  腿 成长 149 / 价值 188。
- ⚠️ 尾部只数显著小于 08-18 定标估算（~1,100-1,400）：差额来自定标探针未含的三道官方筛
  （资格筛剔 ~700 不合格票、全空间 ADV 前 90%、带内后 20%）。属定义的正确后果，非缺陷。

## 1. 修复台账（§4.1：每项 = {错误、独立于 ρ 的证据、改动}）

| # | 错误 | 独立证据 | 改动 | 时点 | ρ 影响 |
|---|---|---|---|---|---|
| 1 | 样本空间混入 `.HK`（旧实现无交易所过滤；`stock_meta` 707 只 .HK 有 `list_date`、无 ST 标记 → 全数通过资格筛） | 规则原文：中证全指样本空间 = 沪深 + 北交；实测 2026-04 截面 465 席挤进前 1500 排名筛、~135 只可进模拟 2000 成分（= 08-19「残余 278 只错位」未解释的一块） | `_space_frame` 交易所护栏 `.SH/.SZ/.BJ`（`a105e77`） | Gate 0A 首跑前 | 0R 实测 2000 带 0.8065 → 0.8007（**−0.0058**）。ρ 微降不构成回滚理由——修复动机独立于 ρ，此例正是该条款的意义所在 |
| 2 | `_linked_members` 联动「新一期」窗写成 `effective_date > eff`（严格大于）——932000 名单 `effective_date` 恰等于调样生效日 → 当期名单被跳过、整条回退旧一期（0B 真值直通全部错位一期） | 0B 启动日志出声告警「932000.CSI 无 2023-12-11 后名单」；官方成分表「纳入日期」= 调样生效日（08-18 已核）；名单生效前公告 → 含当日非前视。修复后冒烟：@2023-12-11/@2024-06-17 各取到当期 2000 只 | 窗下界改 `>=`（`8a3ff21`） | Gate 0A 首跑前——0A/0B 在产生任何 ρ 之前被杀掉重跑，首跑纯洁性保持 | 未单独测量（含在重跑里） |

## 2. Gate 0R 回归护栏（§4.2b，PASS，4653.8s，`gate0r_result.json`）

| 阶段 | 口径 | ρ | 判据 | 结果 |
|---|---|---|---|---|
| 0R-A 锚复现 | 旧口径（含 .HK），v5a 2000 带模拟全窗 | **0.8065** | 0.8046 ± 0.01 | ✓ 代码基线未漂移 |
| 0R-B1 | 现口径（.HK 护栏），同上 | **0.8007** | ≥ 0.7946 | ✓ |
| 0R-B2 | 500 带真值 + T9 全窗 | **0.9636** | ≥ 0.9536 | ✓ 与 08-19 锚一字不差（该路径不经样本空间帧，零漂移的预期精确兑现） |

注：0R-A/B1 之差 = 修复台账 #1 的量化影响。0R 跑于台账 #2 之前，其 2000 带数字为
`> eff` 语义（905/852 为月末快照型，仅个别恰逢调样日的期受 #2 影响，量级未测）。

## 3. Gate 0A 主闸（1000 带，§4.1）—— ⭐ ✅ 首跑 PASS（2026-08-19，2342.0s）

- 判定版 = 官方化模拟（prev 自举、无 000852 真值注入），ρ ≥ 0.85，剔 2026-06-16。
- 事先登记的外推（非测量）：模拟版 ≈0.87~0.88 / 真值版 ≈0.91~0.93。
- **首跑值（永久登记）：ρ = 0.8957 ≥ 0.85 ✓**。n_obs 2715（2015-06-16 ~ 2026-08-18），
  **23 期全建、`skipped=[]`**；分段 CSMAR 0.9050(2379d) / Wind 0.8773(336d)；
  腿只数中位 成长 318 / 价值 356（官方 213/399——G/V 比仍偏成长，登记为得分层残差）。
- 诊断版（000852 真值直通 23 期，不判定）：ρ = **0.8988**（分段 0.9014/0.8926）；
  **模拟成本 = +0.0031** —— 样本空间模拟在 1000 带同样吃平真值（2000 带为 +0.007），
  「官方选样模拟」的通用性第二次得证，**尾部桶（只能模拟）的路径可信度就此坐实**。
- 首跑高于外推上界的解释：外推三点锚基于旧代码（.HK 污染 + 联动窗边界 bug），
  两项修复 + BAND_1000 原文参数（剔后 20% 成交额 / 缓冲 800-1200 / 仅沪深）落地后
  模拟质量高于线性外推。1000 带（无「前 50%」条款）0.8957 vs 2000 带模拟 0.8007，
  差 0.095 —— 与「该条款 + 微盘信噪比构成 2000 带缺口主体」的 08-19 归因相容。
- 产物 `gate0a_result.json` + `gate0a_result_series.csv`。

## 3b. OVERALL Gate 0 = **0A ✓ + 0B ✓ → 通过（2026-08-19）。尾部读数解禁。**

## 4. Gate 0B 副闸（2000 带真值运营期，§4.2）—— ✅ PASS（2026-08-19，557.4s）

- 判据：真值直通、2023-12 起、剔 6-16，ρ ≥ 0.85（08-19 锚 0.8727，含两项修复后重测）。
- **结果：ρ = 0.8710 ≥ 0.85 ✓**。n_obs 649（窗 2023-12-12 ~ 2026-08-18），
  分段 CSMAR 0.897 / Wind 0.858；**零回退告警**——台账 #2 修复后 6 期全部取到当期真值
  （首启时曾整条错位一期）。vs 锚 0.8727 微动 −0.0017 = 两项修复的合计影响，正常。
  产物 `gate0b_result.json` + `gate0b_result_series.csv`。

## 5. 尾部对全窗构建（解禁后，`tail_pair_runner.py`，476.6s）+ ⭐ 结构性发现

产物 `tail_pair_daily.csv`（1016 天，2022-06-14 ~ 2026-08-18）+ `tail_pair_build.json`。

**⭐ 定标阶段漏掉的结构性事实：「3800 名之后」的带在 2022 年之前物理不存在。**
2015~2021 的 14 个调样期尾部空间全部为 0（出声跳过）——资格筛后全市场只数
2015 年 2,349 → 2021 年 3,775，全部落在前 3500 名以内；四母（名义 3800 席）+
排名筛把小市场时代全覆盖。期账：

| 调样期 | 尾部空间 | 腿（成长/价值） | | 调样期 | 尾部空间 | 腿 |
|---|---|---|---|---|---|---|
| 2015-06 ~ 2021-12（14 期） | 0 | 跳过 | | 2024-06 | 388 | 128/137 |
| 2022-06 | 8 | 3/3 | | 2024-12 | 444 | 129/168 |
| 2022-12 | 127 | 48/38 | | 2025-06 | ~470 | 146/177 |
| 2023-06 | 212 | 66/79 | | 2025-12 | ~490 | 134/136 |
| 2023-12 | 336 | 103/126 | | 2026-06 | 520 | 149/188 |

**含义**：① 用户立论的 12.52% 市值缺口是当下真事实，但它是**注册制扩容后近三四年
才长出来的新现象**——第 5 桶是"注册制时代的带"；② 判定公共窗从预期 ~11 年塌缩到
**~3.6 年**（r1 §4「公共窗自首个可用调样日起 ≈2015-06」的前提被实测证伪）；
③ 叠加 `stock_financial` 停更 2025-03-31：窗内「财务新鲜」段只有 2022-12 ~ 2025-03
约 2.3 年，其后尾部对的成长/价值分类吃的是越来越陈旧的封冻财务——**封冻伪影在小盘
杀伤最大**（08-19 消融实测），即候选在 2025-04 后的段位系统性劣化是可预期的。

**用户裁决（2026-08-19）**：窗口起点 = **B（2022-12-12，空间 ≥100 只）**；并给出
方向性裁决——「如果分桶机制已经经过考验，后续方向修改为**按比例调为 5 桶**再试，
不再拘泥于现有 300/500/1000/2000 的模式」。

### 5.1 ⓪ 机器判定结果（`fifth_bucket_formal.py --start 2022-12-12 --n-perm 1000`）

跑前定死的两处适配（在任何 Sharpe 读数产生之前）：① 公共窗起点 = 2022-12-12 +
60 交易日信号暖机（z-score 滚动窗全落干净段）→ 实际 2023-03-14 起 833 日；
② 关2 的 worst(train,val) 窗：r1 冻结日历窗与短公共窗结构性不相容（2014-2020 窗为空
直接越界）→ 最小适配 = **公共窗机械对半拆 H1/H2**（无自由参数）。

| 方案 | S_full | S_H1 | S_H2 | 年化% | MaxDD% | 换手 |
|---|---|---|---|---|---|---|
| 现役（四对） | 1.5561 | 0.5448 | 2.9519 | 26.17 | −16.67 | 10.29 |
| 候选（五对+尾部） | 1.5250 | 0.4957 | 2.9973 | 25.04 | −16.06 | 8.53 |

- 差 = **−0.0311**，`p_selected = p_naive = 0.6144`；关1 ✗ 关2 ✗（worst_tv 0.4957 vs
  0.5448）关3 ✓（回撤更浅、换手更低）→ **OVERALL = STOP**。
- 仓位逐日不同仅 **39/833 = 4.7%** 天——与 08-18 的架构分析一致（1/5 权重 × tanh 界 ×
  等权 × 平滑 × 阈值化，五层把第 5 对压到个位数百分比的影响面）。
- 零 carry 对照差 −0.0411（同号，carry 非主因）。

**⭐ r3 §6 判定 = 情形②**：「在现役架构与本实现下，尾部的增量不可辨认」。
**不得**表述为「尾部无信息」——approximate-PIT、实现差异、1/5 权重稀释、**3.6 年
短窗（现象本身年轻）、财务停更后的封冻段（2025-04+ 占公共窗约 40%）** 都在压效应。
结论 provisional，有效窗截至 2025-03-31。产物 `fifth_bucket_verdict.json`。

### 5.2 本轮沉淀与方向交接

- **永久资产**：官方化管线（Gate 0 四带验证 0.96/0.90/0.87/0.87）+ `tail_sample_space`
  余集构建器 + 尾部对日收益序列（2022-06 起，可续日更攒样本外）+ 双闸执行器。
- **方向交接（用户裁决）**：分桶机制已经过考验（Gate 0 三闸全过）→ 下一步 =
  **全市场按比例重划 5 桶**（对数等距推广到整个划分，不拘泥现有指数带），
  用同一管线建 5 对纯风格，头对头挑战现役。需新预登记（切点定义是核心裁量点）。

## 6. 执行注记

- 0A/0B 第一次启动即被 0B 的出声告警暴露台账 #2，两进程在产生任何 ρ 前杀掉重跑
  ——「空结果/回退必须出声」纪律的直接回报。
- 长跑姿势：harness 后台任务曾在 ~30 分钟被停（非 OOM，RSS ~400MB）→ 改
  `setsid nohup` 脱管 + Monitor 盯日志（mtime 停滞判死；`pgrep -f` 会自匹配勿用）。

## DP 修复后重验（2026-08-21）

- 权威 run：`backtest/output/runs/20260821T210841-p0-revalidation-3030109/`；manifest：
  `backtest/output/runs/20260821T210841-p0-revalidation-3030109/manifest.json`（`status=complete`）。
- 代码 commit：`3030109458a1499ca7601515625fd1ac6dafb025`；seed：`0`。
- 哈希溯源：Gate 0R 输入 `inputs/gate0r_result.json` =
  `d2d31dc7f0e418c939c1ee37d5c85efe6d10fa008be9074b7b4ff051f2ca5ddb`，输出
  `outputs/gate0r_result.json` =
  `d433bc87fd27347aebd955db0a79c9da0bedddd8d1073a8ddcdf8b2d18cdbcc6`；第五桶输入
  `inputs/fifth_bucket_verdict.json` =
  `1e355157b167eaedcfd0cc70307d8565410394bf6cd2adc5186ee1cdff9d173e`，输出
  `outputs/fifth_bucket_verdict.json` =
  `404cc11b515622214cdffdf98e6f6a67caed88b1f6ded0631f21574e94f8f82d`；
  `comparison.json` =
  `cdd537e199f195181a10d8104abad93153cc18df31246b75f324444dca99625e`。
- manifest 的 `metadata.git.dirty=true` 归因来自跑后 provenance review；manifest 明确说明
  跑前没有机器可读的 porcelain 快照，因此该归因不是 run-start snapshot，不能越界解读。
- 本节的 Gate 0R 子节取代本文旧 §2“Gate 0R 回归护栏”作为当前权威；尾部第五桶子节
  取代旧 §5.1“⓪ 机器判定结果”作为当前权威。其他历史段继续保留作历史记录，不由本节改写。

<a id="gate0r-dp-revalidation"></a>
### Gate 0R：旧规格结论翻转，新规格通过

`comparison.json.gate0r` 的机器 disposition 为 `maintained=false`、`flipped=true`：旧规格
`pass=false`，DP 修复后重登锚规格 `pass=true`。这里的“旧”特指同一 DP 修复结果套用退役
锚/地板的机器比较，不改写 2026-08-19 更早一次运行的历史记录。

| 判定量 | 旧规格 | 修复后重登规格 |
|---|---:|---:|
| `repro_hk` | ρ `0.7951`；锚 `0.8046` | ρ `0.7951`；锚 `0.7951` |
| `sim2000_guarded` | ρ `0.79`；地板 `0.7946` | ρ `0.79`；地板 `0.78` |
| `band500` | ρ `0.9698`；地板 `0.9536` | ρ `0.9698`；地板 `0.9598` |
| OVERALL | `pass=false` | **`pass=true`** |

新结果的 `elapsed_s=20697.0`，公共观察窗均为 `2015-06-16` 至 `2026-08-18`，
`n_obs=2715`。因此 Gate 0R 在当前登记规格下通过。

<a id="tail-fifth-dp-revalidation"></a>
### 尾部第五桶：旧结论维持，仍为 STOP

`comparison.json.fifth` 的机器 disposition 为 `maintained=true`、`flipped=false`：旧、新
`OVERALL` 均为 `STOP`。旧规格差 `-0.0311`、`p_selected=p_naive=0.6144`；修复后差
`-0.0055`、`p_selected=p_naive=0.5275`。修复后公共窗为 `2023-03-14` 至
`2026-08-18`（`n_obs=833`），仓位分歧 `39/833` 日，比例 `0.046819`。

| 窗口 | 现役 Sharpe | 候选 Sharpe | 现役 MaxDD | 候选 MaxDD | 现役换手 | 候选换手 |
|---|---:|---:|---:|---:|---:|---:|
| full | `1.5560645051511368` | `1.5505983103792065` | `-0.16666686242381568` | `-0.16138876163900917` | `10.294117647058824` | `8.529411764705882` |
| H1 | `0.5448067428666601` | `0.49227213868506664` | `-0.16666686242381568` | `-0.16138876163900917` | `10.600961538461538` | `10.600961538461538` |
| H2 | `2.9518671239393757` | `3.0436793255871537` | `-0.09187953714142116` | `-0.07015127680314437` | `9.98800959232614` | `6.462829736211031` |

机器裁决措辞保持为：**“② 差异不显著 → 在现役架构与本实现下，尾部的增量不可辨认；
不得表述为「尾部无信息」（approximate-PIT / 实现差异 / 1/5 权重稀释都在压效应）”**。
限定仍是 `provisional: approximate-PIT（CSMAR ann_date 批次日，min(ann,法定截止) 缓解）`。
机器 caveat 原文保留为：`有效窗截至 2025-03-31（stock_financial 停更），其后为数据外推`；
这是对本次重验不适用的 stale legacy metadata。manifest 的 `stock_financial=2026-06-30`
只表示数据库可用性快照；builder 对每个调样期按 `end_date <= review_cutoff(eff)` 过滤
（6 月生效 → 4/30，12 月生效 → 10/31）。最后一期 2026-06 调样实际使用
`review_cutoff=2026-04-30`，tail build log 已明确打印。实际剩余限制是：
历史 CSMAR `ann_date` 用 `min(ann_date, 法定披露截止日)` 修正，超期披露公司仍可能被
当作按时披露，留下 late-filer look-ahead 风险；DP 在 2026 年前使用 CSMAR dividend events，
自 2026 年起使用 `stock_indicator.dividend_yield`。tail 步骤还保留
一次 `overflow encountered in exp` 警告；跑后只确认所有序列化数值有限，不能据此证明
中间溢出没有精度或分支影响，故不得把本次 `STOP` 扩写为“尾部无信息/无价值”。
