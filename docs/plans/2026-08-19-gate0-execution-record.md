# Gate 0 执行记录（r3 预登记 §4，2026-08-19 起）

**预登记**：`2026-08-19-fifth-bucket-preregistration-r3.md`（已冻结）。
**运行器**：`backtest/gate0_runner.py`（0r/0a/0b）；单期核对 `backtest/gate0_smoke.py`。

## 0. 跑前单期核对（§8-④，2026-08-19，`gate0_smoke.json`）

- 1000 带：名单 1000 → 快照对齐 997；因子非空率 EP/CFP/BP/DP 97~100%、SalG/ProG 83.8%、
  dROE 97.2%；腿 成长 314 / 价值 387。模拟 vs 000852 真值重合：prev=None 84.5% /
  prev=真值 92.4%（2026-06）。
- 尾部：520 只（2026-06）/437 只（2024-06），.BJ 31/13、.HK 0；日均市值中位 27.3/22.5 亿、
  最大 43.9 亿（次新巨头被排名筛结构性堵住）；快照对齐 500，SalG/ProG 非空 76%；
  腿 成长 149 / 价值 188。
- ⚠️ 尾部只数显著小于 08-18 定标估算（~1,100-1,400）：差额来自定标探针未含的三道官方筛
  （资格筛剔 ~700 不合格票、全空间 ADV 前 90%、带内后 20%）。属定义的正确后果，非缺陷。

## 1. 修复台账（§4.1：每项 = {错误、独立于 ρ 的证据、改动}）

| # | 错误 | 独立证据 | 改动 | 时点 | ρ 影响 |
|---|---|---|---|---|---|
| 1 | 样本空间混入 `.HK`（旧实现无交易所过滤；`stock_meta` 707 只 .HK 有 `list_date`、无 ST 标记 → 全数通过资格筛） | 规则原文：中证全指样本空间 = 沪深 + 北交；实测 2026-04 截面 465 席挤进前 1500 排名筛、~135 只可进模拟 2000 成分（= 08-19「残余 278 只错位」未解释的一块） | `_space_frame` 交易所护栏 `.SH/.SZ/.BJ`（`a105e77`） | Gate 0A 首跑前 | 0R 实测 2000 带 0.8065 → 0.8007（**−0.0058**）。ρ 微降不构成回滚理由——修复动机独立于 ρ，此例正是该条款的意义所在 |
| 2 | `_linked_members` 联动「新一期」窗写成 `effective_date > eff`（严格大于）——932000 名单 `effective_date` 恰等于调样生效日 → 当期名单被跳过、整条回退旧一期（0B 真值直通全部错位一期） | 0B 启动日志出声告警「932000.CSI 无 2023-12-11 后名单」；官方成分表「纳入日期」= 调样生效日（08-18 已核）；名单生效前公告 → 含当日非前视。修复后冒烟：@2023-12-11/@2024-06-17 各取到当期 2000 只 | 窗下界改 `>=`（`8a3ff21`） | Gate 0A 首跑前——0A/0B 在产生任何 ρ 之前被杀掉重跑，首跑纯洁性保持 | 未单独测量（含在重跑里） |

## 2. Gate 0R 回归护栏（§4.2b，PASS，4653.8s，`gate0r_result.json`）

| 阶段 | 口径 | ρ | 判据 | 结果 |
|---|---|---|---|---|
| 0R-A 锚复现 | 旧口径（含 .HK），v5a 2000 带模拟全窗 | **0.8065** | 0.8046 ± 0.01 | ✓ 代码基线未漂移 |
| 0R-B1 | 现口径（.HK 护栏），同上 | **0.8007** | ≥ 0.7946 | ✓ |
| 0R-B2 | 500 带真值 + T9 全窗 | **0.9636** | ≥ 0.9536 | ✓ 与 08-19 锚一字不差（该路径不经样本空间帧，零漂移的预期精确兑现） |

注：0R-A/B1 之差 = 修复台账 #1 的量化影响。0R 跑于台账 #2 之前，其 2000 带数字为
`> eff` 语义（905/852 为月末快照型，仅个别恰逢调样日的期受 #2 影响，量级未测）。

## 3. Gate 0A 主闸（1000 带，§4.1）——待补

- 判定版 = 官方化模拟（prev 自举、无 000852 真值注入），ρ ≥ 0.85，剔 2026-06-16。
- 事先登记的外推（非测量）：模拟版 ≈0.87~0.88 / 真值版 ≈0.91~0.93。
- **首跑值**（永久登记，无论后续如何）：＿＿＿
- 诊断版（000852 真值直通，不判定）：＿＿＿；模拟成本 = 诊断 − 判定 = ＿＿＿

## 4. Gate 0B 副闸（2000 带真值运营期，§4.2）——待补

- 判据：真值直通、2023-12 起、剔 6-16，ρ ≥ 0.85（08-19 锚 0.8727，含当日修复后重测）。
- 结果：＿＿＿

## 5. 执行注记

- 0A/0B 第一次启动即被 0B 的出声告警暴露台账 #2，两进程在产生任何 ρ 前杀掉重跑
  ——「空结果/回退必须出声」纪律的直接回报。
- 长跑姿势：harness 后台任务曾在 ~30 分钟被停（非 OOM，RSS ~400MB）→ 改
  `setsid nohup` 脱管 + Monitor 盯日志（mtime 停滞判死；`pgrep -f` 会自匹配勿用）。
