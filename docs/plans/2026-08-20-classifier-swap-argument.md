# 换分类器维度 ·「逃出平均化钝化」论证（2026-08-20）

> 背景：08-19 分桶结构维度总账（`2026-08-19-geometric-5buckets-verdict.md` §跨轮）要求
> 该族维度再开候选前先回答「为什么这次能逃出平均化的钝化」。用户 08-20 裁决
> **论证先行**：零额度纸面工作，立得住才起草预登记，立不住则把维度也记档。
>
> **纪律声明**：本文档只读已有文档与已落盘产物（07-08 B1/B2 文档、validation CSV、
> 08-17/18/19 收官文档），**未跑任何新回测、未产生任何新策略读数**——无污染。

> **⚠️ 同日更正（用户驳回后审计，见 §7）**：本文档首版把 §2.2（行业提纯）定级为
> 「已测，败于信号-标的匹配」——**该定级经过程审计后不成立**，7 月对提纯步的检验
> 无统计功效（仓位分歧仅 9.6% 天 + 无配对检验 + −0.03 点估计），「失配」解释也从未
> 对交易标的测过。§2.2 子路线回到「未经干净测量」状态。§2.1 / 2.3 / 2.4 的关闭不受
> 影响。原文保留供对照，以 §7 为准。

## 0. 结论先行

**~~立不住~~（首版结论，§2.2 部分已被 §7 审计推翻）。** 首版判定：这个维度已在
2026-07-08 被「方向 C」B1→B2→切主三步完整走过，并在净值层清晰证伪（1.01 / 0.98
vs 1.39）。08-19 总账把「换分类器」登记为"唯一未测正交维度"是**记账遗漏**：
正交属实，「未测」不实——当时没有连上 7 月方向 C 的账
（B1 复现 → B2 分解 → 切主证伪 → rotation 证伪，四步闭环，
`2026-07-10-optimization-roadmap-retrospective.md:35`）。

**修订后结论**：换测量类子路线（2.1/2.3/2.4）维持关闭；**行业提纯（2.2）是
「机制上逃得出钝化 + 从未被有功效实验测过」的唯一活口**，且 8 月官方级复刻管线
（Gate 0 已验）恰好补上了当年实验最大的底座缺陷——改进方案见 §7.4，待用户裁决。

## 1. 钝化机制的精确内容（论证的靶子）

10 个实测点拆成两半，各自压死一类操作：

| 半边 | 实测点 | 内容 |
|---|---|---|
| **减少平均 → 变差** | 5 点 | 08-17 分批探索：5 种快腿（raw/短平滑）全劣于 5 日平滑（跨时间平均） |
| **换构成 → 不可辨认** | 5 点 | 08-17 §6b：4 个标的上匹配单对全劣于四对等权（跨标的平均）；08-18 族统一②；08-19 尾部对叠加②、等比 5 桶全替换②（后两者仓位仅 4.7% / 8.8% 天不同） |

机制的物理内容：**平均消独立噪声、保公共成分**。现役信号 = 4 对价差斜率的
平滑等权平均，再过 tanh 界 + 阈值。凡是只改变「被平均的东西的组成」而保留
公共成分的候选，架构把它压成几乎同一个仓位序列 → ②。

**由此，逃出钝化的唯一逻辑形态：候选必须改变公共成分本身（改内容），
而不是换一种方式测量同一个东西（换测量）。**

## 2. 换分类器的全部子路线 vs 已有实测

「用自有因子层替代中证方法论选腿」拆开只有四条子路线，逐条对答：

### 2.1 换测量——自建因子层复测同一个 G−V 轮动

- **B1（07-08）**：自建全市场打分篮子（6 因子截面 Z 合成、月度 formation、
  Top/Bottom 30%，数据源/缺失处理/PIT 全自主）——**信号级 ρ vs 生产
  equal_weight factor_value = 0.88~0.91（U0~U3）**（`output/style_basket/
  validation_all.csv`）。这是分类器自由度的直接测量：换一整套自建方法论，
  信号收敛回同一条。
- **族统一（08-18）**：官方两套方法论互换（经典族↔纯风格 P 族）= ②。
- **切主同秤（07-08，`2026-07-08-b2-industry-neutral-decomposition.md` §4）**：
  self_mixed_U2 full Sharpe **1.01 vs 生产 1.39**——自建非但无增量，还背上
  0.38 的自建损耗（数据底座缺陷 + 丢失四对平均）。信号相关 0.906、同向日 85.5%。

→ **双重死：机制预测②，实测更差。**

### 2.2 改内容——行业提纯（机制上唯一真逃生口，已测，败于别处）

- **B2 分解**：v1 混合价差年化波动 13.3% ≈ v2 纯风格 7.7% + 行业轮动 6.8%，
  官方指数对日收益 R²=0.77 被两分量解释，corr(v2, rot)=0.65 **跨带共振**
  → 行业轮动是四对平均**消不掉的公共污染**。提纯是唯一「改公共成分」的杠杆，
  钝化机制管不着它——这一点本论证承认。
  （注意用语：B2 的「纯风格」= 行业中性提纯的自建篮子，与中证「纯风格指数」
  的命名无关。）
- **但净值层已测**：IC 确实升（0.179 > 0.126，6/6 组目标、分窗排序均成立）
  **而净值不转化**（pure 0.98 ≈ mixed 1.01 ≪ 1.39）。败因当年记为决策级
  方法论发现：**IC 的预测目标是风格轴自身，净值的交易标的是宽基
  （风格+行业+β 混合体，方差分解已证约各半）——宽基择时恰恰需要行业轮动
  成分，提纯反而造成信号-标的失配**。换手相近（22.5 vs 21），非成本问题。

→ ~~**逃出了钝化，撞死在信号-标的匹配上。**~~ B2 已于 08-12 复盘 CLOSE
（「纯风格 = 测量仪非择时器」，`2026-08-12-project-review-and-priorities.md:82`）。
**【§7 更正】该 CLOSE 的证据级别经审计不足以支撑「败于匹配」的定论——
当年对提纯步的净值检验无功效、「失配」解释未经检验，本子路线状态改为
「未经干净测量」。**

### 2.3 提新鲜度——更快重分类（官方半年调样 → 自建月/周调）

本质是**减少时间平均**。5 个快腿点 + 08-17 定论「提高信息新鲜度换不来收益：
抢到的是噪声」直接压死。分批建仓线 08-17 已把「快信号」机制升格为被证伪
（剔 2022-03 后快信号下穿与随机无异）。

### 2.4 提纯度——更极端分位、更小篮子

腿内只数下降 = **减少截面平均** → 机制预测更噪；且改的是价差**振幅**不是
斜率**符号**，tanh+阈值架构对振幅不敏感。无专项实测，但两头都撞机制，
且小盘带财务数据质量最差（封冻伪影杀伤最大），无立项价值。

### 2.5（不属本维度）换因子菜单——用非 G/V 语义的因子定义新「风格」

那不是「换分类器」，是**新信号线**：须自带「存在第二条可择时的轮动」的独立
证据，目前手上没有；信号轴总账（①②④⑤⑦ + 分批 + 匹配单对）全线反向。
若将来要开，按新信号线立项，不得借「分类器」名义绕开信号轴关帐。

## 3. 「公平版」候选的预期与成本（为完备性补齐）

对 2.1 最强的辩护是：切主用的是**单一 universe 价差**，丢了四对平均，输的
0.38 里有一部分是架构损耗而非分类器损耗。公平版 = 分带自建腿 ×4~5 对、保住
四对平均。预期与成本：

- **预期 = ②**：B1 分带价差日频 ρ 0.60~0.87，等权平均后信号级 ρ 必然回到
  ~0.9（B1 已实测 0.88~0.91），收敛回现役仓位序列——正是钝化机制的标准形态。
- **成本**：周级建造 + 自建数据底座缺陷全套照背（`stock_financial` 停更
  2025-03-31 + 封冻伪影、SalG 冻结 2025Q2+、无历史 ST 过滤、总市值≠自由流通
  排名）。**候选不对称教训的极端形态：现役腿是官方算好的成品（零建造、零复现
  损耗、数据永远新鲜），自建腿先把 7 月量过的损耗背上再谈优劣。**

期望值显著为负。不立。

## 4. 判定

| 子路线 | 机制判定 | 实测状态（§7 修订后） |
|---|---|---|
| 2.1 换测量 | ② 预测 | **已测，输**（1.01 < 1.39）——维持关闭 |
| 2.2 改内容（提纯） | 逃出钝化 ✓ | ~~已测败于匹配~~ → **未经干净测量**（§7） |
| 2.3 提新鲜度 | 撞时间平均 | 机制已被 08-17 分批线证伪级关闭 |
| 2.4 提纯度 | 撞截面平均 | 未测，无立项价值 |
| 2.5 换因子菜单 | —— | 不属本维度，须按新信号线自证 |

~~**「逃出平均化钝化」之问答不出肯定答案。换分类器维度记档。**~~
**【§7 修订】2.2 是唯一同时满足「机制上逃得出钝化」与「从未被有功效实验测过」
的活口，改进方案见 §7.4。其余子路线维持关闭。**

## 5. 重开钥匙（事先写死，防低标准重开）

任一满足才值得重开本维度：

1. 出现一个**与现役信号相关 < 0.7** 且月频 IC 独立为正、分窗稳定的自建风格
   读数（现有全谱系 0.877 以上；唯一 <0.7 的 U4=0.63 是数据质量最差带且
   单对形态撞机制，不算）。
2. 财务数据底座修复（PIT 首披日回填 + 停更恢复）之后，小盘带出现自建分类器
   与官方腿**方向性分歧且事后自建对**的实证案例。
3. **执行标的变成非宽基**（如真正的风格腿多空组合）——那时 §2.2 的失配反转，
   纯风格测量仪才有用武之地。但那是另一个策略的立项，不是现役换腿。

## 6. 对 08-19 总账的更正

[[project-replication-improvement-2026-08-19]] 明日入口把换分类器写成
「登记过的正交维度……不受本轮结论压制」——**正交属实，「未测」不实**。
7 月方向 C 四步闭环就是这个维度的完整一轮，且其证伪强度（净值层输 0.38、
提纯 IC↑不转化）**高于**分桶维度的②。本文档是把两本账连起来，不含新证据。

连账后的全景：**「腿怎么选」（本文档）与「桶怎么切」（08-19 总账）两个维度
都已关闭**——信号侧对「构成」与「测量口径」双重不敏感，价值全部来自
多重平均降噪 + 官方腿的成品质量。这与 08-17 推论「本候选的增量只能来自
执行侧」再次汇合。**【§7 更正：上段「都已关闭」限缩为「除 2.2 提纯子路线外
关闭」。】**

---

## 7. 同日过程审计（用户驳回「已测败于匹配」定级后，2026-08-20）

### 7.0 触发与纪律

用户裁决：§2.2 的定级下得草率，先审 7 月过程有无明显漏洞，再讨论改进。
本节新增的量全部是**信号层数据性质**（已落盘 CSV 之间的相关与符号分歧、
git 历史考据），未产生任何新策略收益读数。

### 7.1 不成立的嫌疑（还 7 月清白，防止过度翻案）

- **ann_date 批次日陷阱不适用于 B1**：`signals/common/financial_reader.py` 初版
  （`91922c3`，07-07）即带 `min(ann_date, legal_disclosure_deadline)` cap；
  07-11 `f7ad5b1` 只是 docstring 纠偏。B1 打分没有丢最近一季财报。
- **IC 优势的一致性是真的**：6/6 组目标方向一致 + 分窗（2014-19/2020-26）排序
  成立。但注意它同样没做过显著性（n=149 月，Spearman 差 0.053 < 单边 se≈0.082），
  两边都要按同一标准打折。

### 7.2 成立的漏洞（四条硬伤）

**L1 提纯步从未有过有功效的检验（最重）。** 实测（本日，committed CSV 公共窗
n=3,036，2014-01→2026-07）：corr(mixed, pure)=0.9385，**离散仓位分歧仅 9.6% 天**
（ew vs mixed 13.4% / ew vs pure 14.6%，与 B2 文档「同向日 85.5%」互证）。
功效锚（08 月 ⓪ 机器实测）：geo5 在 2656 天窗、8.8% 分歧天下，|ΔSharpe|=0.196
连反向单尾都只贴边（0.065）——**~10% 分歧天的候选，可辨认门槛在 0.2 量级**。
当年提纯步的 −0.03（0.98 vs 1.01）是不可辨认区深处的一个点估计，且**没有做
配对检验**（bootstrap 500 只测了 vs 随机；paired_bootstrap 与 ⓪ 机器是 8 月才立的
规矩）。按今天的标准这就是一个 ②（不可辨认），却被升格成了「IC↑不转化」的证伪。

**L2 「失配」解释是事后叙事，从未检验。** `decompose.py:102` 实证：IC 目标只有
`["blend","500pair","1000pair"]` 的**指数对价差**——rotation 分量对**宽基交易标的**
的择时价值从来没测过。「宽基择时需要行业轮动成分」是对一个无功效 null 结果的
机制化解释，证据级别不够。

**L3 提纯只在混杂底座上测过。** 挑战者 = U2 单带（丢四对平均，§6b 量级 0.2~0.5）
× 粗分类器（总市值排名、月度 formation、无 ST 筛、SalG 冻结尾巴），0.38 的底座
落差把台阶淹没。**提纯从未在官方级底座 + 保留四对平均的形态下测过**——而 8 月
的官方化管线（Gate 0 四带 ρ 0.80~0.96，样本空间重合 86.1%）恰好把这个底座
造出来了。

**L4 参数先验失配。** 20d40z+sm5 是在官方混合对（年化波动 13.3%）上调出的甜点，
直接搬到波动减半（7.7%）、结构不同的提纯价差上；当年文档自己登记了
「参数先验含全样本信息、风险低非零」。

（次级 L5：07-08 评估早于 07-11 blend carry bug 修复——三方同秤故相对序大体稳，
但绝对数与后来口径不可比；行业标签 2021 前静态外推，提纯质量在前半窗打折。）

### 7.3 修订后的判定

- 2.1 / 2.3 / 2.4 关闭**维持**（B1 的 ρ 0.88~0.91 收敛测量与机制论证不受本审计影响）；
- **2.2 状态改为「未经干净测量」**：机制上它仍是唯一改公共成分的杠杆（corr(v2,rot)
  =0.65 跨带共振；对最终信号扰动 9.6~14.6% 分歧天，高于全部 ② 候选的 4.7~8.8%）——
  「逃出钝化」之问对它答得出肯定答案；
- §5 的三条重开钥匙是按首版（错误）定级校准的，对 2.2 **由 §7.4 方案取代**；
  对 2.1/2.3/2.4 仍有效。

### 7.4 改进方案骨架（待用户裁决，未起草预登记、未跑任何闸）

**候选形态**：官方级复刻腿 + **带内行业中性化** × 4 带等权——架构其余一切不动
（带、平均、平滑、阈值、执行姿势），唯一改动 = 选样步加行业中性
（`select_baskets_industry_neutral` 语义移植进 `pure_style_builder`）。

设计要点（针对 L1~L4 逐条补）：
1. **底座**（补 L3）：未提纯版就是 Gate 0 已验的官方化管线，复现损耗有量化锚；
2. **判定**（补 L1）：⓪ 机器配对 bootstrap + 事先 margin；**功效预检写进预登记**：
   按预期 ~10% 分歧天，可辨认门槛 ≈0.2 Sharpe——须事先接受「|Δ|<门槛 → 判②」；
3. **机制关 preflight**（补 L2，最便宜、可先行）：rotation 分量对宽基交易标的的
   直接 IC——若 rotation 对宽基有独立正贡献，「失配」解释成立、候选先验降；
   若无，提纯先验升。这一步只动已落盘序列，零建造成本；
4. **参数**（补 L4）：裁决点——沿用生产参数（保守、可比）vs 按候选价差波动
   重标 z 窗（须预登记，防拟合）；
5. 行业标签 PIT：2021 前静态外推 → 判定窗证据分级或限窗（裁决点）；
6. 成本：库内零 Wind 额度；构建量级同 geo5（全窗 ~60-100 分钟级）+ 选样改造与测试。

**诚实先验（写给未来的自己）**：7 月点估计是 −0.03；IC 优势未做显著性；优势
集中 2020+。本方案的价值主张不是「预期大概率①」，而是「该杠杆是仅存的活口 +
实验这次可以做干净 + 成本低」。若再得 ②，则 2.2 以有功效实验关闭，整个维度
才真正记档。

### 7.5 机制关 preflight 设计（用户 08-20 裁决先跑此步；**判读规则跑前冻结**）

**命题**：检验 L2——「宽基择时需要行业轮动成分」（7 月失配解释）是否为真。
7 月只测过三分量对**指数对价差**的 IC，从未对**宽基交易标的**测过。

**输入（全部库内/已落盘，零 Wind 额度、零新建造）**：
- 分量：U2（主，7 月判定所在带）与 U0（副，全市场稳健性）的 v1 混合 / v2 纯风格 /
  rotation=v1−v2，信号化 = `decompose.py::_signal` 原函数（20d40z+sm5 生产口径）；
- 目标：**宽基现货日收益**（`backtest.data.load_underlying_returns`，与 baseline 秤
  同一标的层）——blend（主，= 500/1000 对半，7 月切主 headline 所在口径）+
  500 / 1000（副）。不含 carry：carry 是执行层，机制关只问预测力。

**方法**：与 `decompose.py` ③ 完全同法（月末信号 vs 次月收益和、非重叠、
Spearman 主 / Pearson 副；日频 d5/d10/d20 参考），分窗 2014-19 / 2020-26 同 B2。

**两个锚（先看锚后看新列）**：
① 接线锚 = 复现 decompose.py 已发表值（v1/v2/rot 对指数对 blend 价差月频 Spearman
0.126 / 0.179 / 0.050），对不上即停；
② 量级锚 = 生产 ew 信号对宽基 blend 的月频 IC（「有效信号该有的量级」参照）。

**判读规则（冻结）**：
- **Q1（rotation 独立价值）**：rotation 对宽基 blend 全窗月频 Spearman IC ≥ +0.05
  且两分窗同号 → 「宽基需要行业轮动」成立。
- **Q2（提纯是否伤宽基预测力）**：IC(v2) − IC(v1) 对宽基 blend ≥ 0 → 提纯不伤。
- Q1 否 & Q2 是 → **失配解释被否，提纯候选先验↑**（转预登记起草）；
  Q1 是 & Q2 否 → **失配解释成立，提纯候选先验↓**（建议停）；
  其余组合 → 中间态，报用户裁决。
- 显著性参照 se≈1/√n_months≈0.08；一致性（跨目标/跨窗/跨 universe）为实际标准
  （与 B2 同标准）。0.05 边界沿用 B2 对 rotation「无独立预测力」的同一边界。

**执行**：探针 `backtest/rotation_target_probe.py`，产出
`backtest/output/rotation_target_probe.csv`；本节先于运行提交（git 时间戳为证）。
