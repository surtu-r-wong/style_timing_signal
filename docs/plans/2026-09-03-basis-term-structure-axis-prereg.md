# 期指基差期限结构面·入场券探针预登记（2026-09-03，**用户已冻结，跑前未改**）

## 0. 立项

07-09 长轴探针 C1/C2 只测了**主力合约**年化基差率的水平与 20 日变化（STOP，C1 最接近 p 0.090）。
`public.futures_daily` 现每日 IC（2015-04 起）/ IM（2022-07 起）恰有 4 个合约且全带 oi，**跨合约的期限结构**（贴水在不同到期上的分布）从未测过。
它刻画的是对冲需求的**期限分布**，与主力合约水平是不同的量。用户 09-03 裁「继续」（三块未测面的第三块）。

## 1. 命题（双侧）

基差期限结构（斜率 / 远近差 / 斜率变化）是否对 equal_weight 生产信号（long-flat，blend）提供显著、独立、两半窗稳健、成本后存活、且经选优校正仍显著的择时信息。

## 2. 数据与构造（冻结）

- 每日每合约年化基差率 `b = (S − F)/S × 365/days`（正 = 贴水），`days` 到第三个周五，与 `backtest.data.annualized_basis` 完全同口径；F 取合约 close，S 取 index_daily 收盘（500→000905，1000→000852）。
- **剔除距到期 < 7 个自然日的合约**（年化在到期周爆炸）；剩余合约 < 3 个的日子记 NaN。
- **T1 期限斜率**：剩余合约上 b 对到期时间 T（年）的 OLS 斜率（单位：年化基差率 / 年）；正 = 越远期贴水越深。
- **T2 远近差**：最远合约 b − 最近合约 b。
- **T3 斜率变化**：T1.diff(20)。
- **口径合并**：500 与 1000 各自计算后取可用腿均值（2022-07 前只有 IC，即 IC 单腿；之后两腿平均）。这是信号量的合并，不是 P&L 的 carry 合并，故不按 `blend_carry` 的缺腿补 0。
- PIT：合约 close 与指数收盘同日可知，T+1 生效，无滞后。

## 3. 网格、窗口、秤与闸门（冻结）

- 三族 × `GRID_LEVEL`（lb∈{5,20} × zw∈{60,250}）× k∈{5,10,20,40} = **48 变体**。
- 两半窗用默认 `2014-2019 / 2020-2026`（数据 2015-04 起，首半窗约 4.7 年）。秤：blend 标的 + carry、3 bps、T+1；置换 1,000。
- 关 1～3 = `run_families_probe` 原样；**关 0（候选 ⓪）** = 48 变体全窗 |IC| argmax 的循环移位空分布，族代表 |IC| ≥ 95% 分位。四关全过才 PASS。

## 4. 功效边界（事前）

约 2,700 个交易日；k=20 约 135 窗、k=40 约 67 窗——是三块面里样本最长的；可检出 |IC| ≳ 0.15。

## 5. 产出与引用纪律

机器 `backtest/basis_term.py`（序列 + 缓存 `backtest/output/basis_term_series.csv`）、`backtest/basis_term_probe.py`；输出 `basis_term_probe{,_verdicts}.csv`、`basis_term_selection.json`。
关的是「IC/IM 期限结构三族 × 网格对 equal_weight 的入场券」；不得引作「基差信息无价值」（C1 复检另按量化条件等样本）。

## 6. 数据就绪度（2026-09-03 实测，只读）

| 项 | 值 |
|---|---|
| 序列 | 2,767 日（2015-04-16～2026-08-31），T1/T2 无缺日，T3 仅 20 日预热 NaN；每日 4 合约、剔近月后 n_used 3～4 |
| T1 分布 | 中位 −0.056，四分位 −0.156～+0.025（年化基差率/年）；\|T1\|>0.5 的 164 日、>1 的 56 日 |
| 极值来源 | 全部为 2015-06～09 股灾期（如 2015-07-08 T1 = −6.86：近月极端贴水、远月相对温和），是真实事件不是毛刺 |

冻结规则：极值**不裁剪、不剔除**——`level_signal` 的 rolling z + tanh 已是有界变换；2015 段落在首半窗内，其对 zw=250 形态的
z 尺度影响持续到 2016 年中，结果文档须报告 2015～2016 段的 IC 与其余年份是否同号。
