# 新轮动轴入场券·批次二：质量轴（设计与判读规则，跑前冻结）

日期：2026-08-24。状态：**冻结于运行前**。母设计 =
`2026-08-24-new-rotation-axes-entry-ticket.md`（§1 构建、§2 判读、§3 证据形态
全部继承，本文只登记增量）；批次一已收官（四轴 ALL_FAIL，见母文档 §4）。

## 1. 增量规格（冻结）

- **轴**：质量。因子 = **ROE_TTM 水平** = 净利润 TTM（`income.B002000000`，
  `_ttm_latest`）÷ 最新净资产（`balance.A003000000`，取 `end_date` 最新行，
  与腿工厂 BP 分子同源同 PIT）。**净资产 ≤ 0 → NaN 剔除**（ROE 无意义）。
  单因子、无网格、无复合。
- **PIT**：`_fetch_series` 修正可知日 = `min(ann_date, 法定披露截止日)`
  （腿工厂现状机器）。**残余限制**：超期披露公司会被当成按时披露 →
  结论标 provisional（与 B3/Gate 0 同款措辞）。
- **canonical 方向**：高 ROE 腿 − 低 ROE 腿。
- 其余全部继承母设计 §1：四带 × 半年调样 2015-06 起 × 三分位等权漂移腿，
  MIN_LEG=10 无静默截断。

## 2. 判读（继承 + 一处登记）

- 主判读与两锚逐字继承母设计 §2（blend / k=20 / 控现役 ew / 双侧置换
  p<0.05 / n_perm=2000 / seed=0 / 评窗 2015-08-15 起）。
- **多重比较登记**：本轴是方向一全族第 5 个检验。逐轴裸 p 判定沿批次一冻结
  规则不变；Bonferroni 参考列自本批起按**全族 ×5**（p<0.01）报告，仍不改判。
- 功效预检同母设计（n≈132，可辨 |pIC|≳0.17），未过闸措辞同款冻结。

## 3. 证据形态

complete immutable run（同母设计 §3；构建命令带 `--axes quality`）。
财务取数重：估 ~1 h 本机后台。零 Wind 额度。

## 4. 执行记录（2026-08-24，跑后追加；§1–3 冻结原文未动）<a id="b2-execution"></a>

- 权威 run：`backtest/output/runs/20260824T102455-axes-ticket-e2df789/`
  （`status=complete`，13 分钟，spec commit = 运行 commit = `e2df789`，seed=0；
  23 期 × 4 带 = 92 期带零跳过；两锚全过）。
- 首次派发 `20260824T100630-…` 在 ~5.5/23 期处被外部 SIGKILL（非 OOM、非代码
  崩溃；用户确认非主动停止后重派）；该目录 manifest 已 post-hoc 补记
  interrupted、不构成证据、留宿主本地不入库（165746 判例同款处置）。
- **判定：未过闸**（措辞按母设计 §2 冻结：当前功效下不可辨认）：
  偏 IC **+0.099 / 双侧 p=0.234**（n=132）；裸 IC +0.130(p 0.135)。
  参考行无特殊形态（h1 −0.017 / h2 +0.093；k=5/10 近零；500: 0.139 /
  1000: 0.076）。结论标 provisional（超期披露残余限制，§1）。
- 登记瑕疵：`axis_ticket_verdict.json` 内部 `spec` 字符串为模块常量、指向
  母设计路径；权威溯源以 manifest `metadata.spec`（= 本文档）为准。
- **方向一入场券全族至此扫完：5 轴 0 过闸**。唯一贴线者仍是股息轴
  （批次一 +0.160/p 0.063）。后续处置待用户裁决。
