# Probe ⑦ 预登记（草案）：公募持仓拥挤面（⑦B 风格暴露回归 + ⑦A 季报仓位/板块分位）

> 状态：**草案**。冻结 commit 前置条件：① 国证两腿 CSV 入库并验证；② ⑦A1 额度档用户拍板并拉数落缓存。
> **冻结必须发生在任何信号-收益配对计算之前**（数据回填/入库本身不构成看结果）。
> 立项与四问裁决：`2026-08-14-fund-crowding-agenda.md` §7。闸门框架沿用 ②/面2（三关 + ⓪ selection-aware 置换机器 + 关1b 偏 IC 控 equal_weight）。

## A. 数据实测更新（2026-08-14，推翻立项材料引用的旧文档快照）

- `fund_stock_holdings` 实测 **41 期连续（2016-03-31 ~ 2026-03-31），无 2020Q1/Q2 缺口**（逐期 distinct fund_code：2016Q1 583 → 2026Q1 6,201）；**NULL `ann_date` 实测 0 行**（08-05 手册记录的 414 行已被后续补齐）。→ 立项裁决 3 的两笔回填**取消**，wset 零消耗。
- `fund_meta` 几乎全库为主动权益两类（偏股混合 5,344 + 普通股票 1,089，其余 3 只）。
- `index_daily` 腿：`885000.WI` 2015-01-05 起 / `000300.SH` 2014-01-02 起 / `000852.SH` 2013-03-06 起，均到 2026-08-11；国证成长/价值不在库（待用户 CSV）；`932000.CSI` 仅 40 天（本批不用）。

## B. 自由度决议（冻结清单）

**⑦B 族（日频·滚动风格暴露分位·16 点）**

1. 回归：滚动 OLS `r_885000 = α + β_m·r_mkt + β_s·spread`，**W=240 固定**（信达 20220916 回看 240 日先验；W=120 仅作敏感性报告列，不进选优）。
2. 两个维度：size 维 `spread = r_000852 − r_000300`，`r_mkt = (r_000300+r_000852)/2`；growth-value 维 `spread = r_国证成长 − r_国证价值`，`r_mkt = 两腿均值`。
3. 信号：`β_s` 的 rolling percentile，zw ∈ {250, 500}；持有 k ∈ {5, 10, 20, 40}。
4. 网格 = 2 维 × 2 zw × 4 k = **16 点**，合成单次置换调用。
5. 方向先验入档（高暴露分位 → 该风格价差未来反向，负 IC），**检验双侧**。
6. 有效起点：β 首点 ≈ 2015-12（885000 起点 + 240 日）；分位 burn-in 后 zw250 ≈ 2016-12 起、zw500 ≈ 2017-12 起。

**⑦A 族（季频阶梯·历史分位·6 点）**

7. A1 = 主动权益两类（`ACTIVE_FUND_TYPES`）的 `prt_stocktonav` **每期中位数**；抽样档见 §C（待用户拍板）；抽样规则 = 对 fund_code 取 md5 哈希升序取前 N（确定性、与成立年代无关，避免"代码字典序=基金年龄"的幸存者偏差）。
8. A2 = 重仓池板块占比：`Σ hold_value(创业板 300/301.SZ + 科创板 688.SH) / Σ hold_value(全部 A 股)`，港股/北交所行剔除；**小盘成长混合代理，语义不纯已由用户接受**（裁决 2）。
9. 分位 = expanding percentile，burn-in 8 期（首个可用值 2018Q1）；k ∈ {20, 40, 60}（季频慢变量，短 k 无意义；k=60 超项目常用集，理由在此登记）。
10. PIT 生效日：期级序列在**当期样本 `ann_date` 的 P95** 日生效（中位数与 P95 对称的稳健口径）；敏感性列 = 报告期末+30 自然日固定规则。`ann_date` 一律复用 `fund_stock_holdings` 同 (fund_code, report_date) 行（实测无 NULL）。基金季报每季披露资产组合（Q2/Q4 亦有季报），`prt_stocktonav` 按 rptDate=季末、季报口径统一。
11. 网格 = 2 序列 × 3 k = **6 点**，合成单次置换调用。
12. **低功效声明**：41 期 − burn-in 8 期 ≈ 33 个独立观测；结论表述限"低功效未检出/检出"，**不得写"无价值"**（ERP 年度口径先例）。

**共同**

13. 两族**分族跑 ⓪ 机器**（各自合成单次调用），族级 Bonferroni **α=0.025**（②接入三规矩的预登记分支）；`min_shift=2·max(k)` 规矩照抄。
14. 三关：关1 p_selected<α **且**偏 IC（控 equal_weight，与 ②/面2 同一目标序列与机器）同号 p<α；关2 两半窗同号（按各族自身样本切半）；关3 成本后净 Sharpe>0（3bp）。任一不过 → STOP 归档。
15. 报告列（不设闸）：MaxDD/Calmar、corr(⑦B, 现役四对信号)、corr(⑦B, ⑦A)、W=120 敏感性、PIT 敏感性（P95 vs +30 天）、2024-26 第二验证窗、concentration_summary。
16. train/val：沿用 train 2014-20 / val 2021-23（数据晚起点如实少覆盖）；2024-26 仅报告。
17. 过闸后不动部署；环境变量仓位调节另立批次（衔接候选 ①）。
18. 预算上限：数据入库后实现+正式跑 2 个工作日、QA 双审 1 天；到点未出裁决按 STOP 收摊。

## C. ⑦A1 额度档（待用户拍板）

wss `prt_stocktonav`，1 字段 × 逐期基金数（rptDate=41 个季末，ann_date 零额外消耗，复用库内）：

| 档 | 口径 | 额度 | 备注 |
|---|---|---|---|
| **抽样 500（推荐）** | 每期 md5 哈希前 500 只 | **≈20,500 格** | 中位数抽样误差 ~1/√500，几乎无损 |
| 全量 | 每期全部在世主动权益 | **≈110,353 格** | 逐期实测合计 |

拉数落 `style_timing_signal` 本项目 csv 缓存（fund_code, report_date, stock_to_nav_pct, ann_date），登记为永久资产；**零 DDL、零跨项目写表**（不动 `fund_holder_structure`）。

## D. 用户 CSV 清单（⑦B 成长/价值腿，Batch 13 先例灌 `index_daily`）

| 指数 | 代码（Wind 检索为准，保留 CSV 实际代码列） | 区间 | 字段 |
|---|---|---|---|
| 国证成长 | 399370.SZ（国证官网 CN6001，安信笔记口径） | 2013-01-01 ~ 今 | 日期、close（有 OHLC/volume/amount 更好） |
| 国证价值 | 399371.SZ（CN6002） | 同上 | 同上 |
| （可选顺带）中证2000 | 932000.CSI | 基日 ~ 2026-06-15 | 补库内 40 天断头；**非本批必需** |

入库后与既有行验证（重叠区零容差先例），`index_code` 以 CSV 实际代码冻结入本文档。

## E. 冻结时待填

- [ ] 国证两腿实测覆盖（起止/行数/代码）
- [ ] ⑦A1 档位与实拉行数、缓存文件路径
- [ ] ⑦B 各维度有效样本起止实测
- [ ] commit hash
