# 信号生成数学三族预筛 —— 固定规格关账登记（2026-08-26）

> **一句话**：本档实测了固定的 EWMA-std、CUSUM、Hamilton，以及 raw/DEMA(5)/DEMA(7)
> 插点。EWMA-std 是现役近克隆（分歧 0.35%~1.04% 天）；固定 CUSUM/Hamilton 分歧够大
> （19%~47%）但未兑现预先写定的转折期故事。结论只关闭这些固定规格，并把同类再参数化
> 降为低优先级；不构成对整个生成数学方法族的实证关账。预筛只归档不批准，全程未看任何
> 带方向的收益差。

---

## 0. 背景与命题分层

用户问题（2026-08-26）：现役信号生成骨架（价差 → 线性因果滤波/动量 → z-score →
tanh → 阈值/状态机）之外，是否还有其他数学方法。

命题分层：**本档只覆盖「同输入、换加工数学」这一层**。「换输入信息」（五轴、广度、
拥挤等，各有精确scope）与「换执行标的」（08-17 双通道已按两种正式秤STOP关账，但留有
小幅增量迹象）在别的账上。

候选族的筛选逻辑（跑之前定的，不是事后总结）：

- 本档的线性因果滤波先验是：许多再参数化可能成为现役近克隆。EWMA-std被选作一个固定
  代表实测；卡尔曼、因果小波/SSA、分数差分和OU拟合并未逐一实测，因此只降低优先级，
  不登记为实证失败。
- 非线性部分实际只测了固定 **CUSUM** 与 **Hamilton** 规格。ML族未实测；由于20日持有期
  下有效独立观测仅约1.5e2个、置换选优功效先验不足，本轮不立项，但不能写成实证死路。
- 两侧HP、EMD会引入前视，本任务直接排除；若有严格单侧实现，须作为新规格另行审查。

## 1. 纪律与秤

- 两把秤（`feedback-timing-prescreen-two-dims`）：**功效 = 仓位分歧天数**；
  **容量 = 分歧生效日 |r| 量级与聚集**。预筛**只能归档，不能批准**。
- 规格全部**事前固定**，每族**跑一次**，不看结果调参、无任何扫描。
- 前置自检：平坦版（现役规格重实现）必须对 committed 信号 CSV **同日期逐位一致**
  才开跑 —— 三条线实测均 0 不一致（equal_weight 3074 / citic40d 3982 / hybrid20 3732 天）。
- 仓位口径 = `backtest.positions.production_position`（long-flat，>0），T+1 生效
  （`engine.run_strategy` 同秤）；对比标的收益 = `load_underlying_returns`（500/1000/blend）。

## 2. 线性滤波族：EWMA-std（族代表）

**规格**：仅把三条线 z-score 的**分母** `rolling(w).std()` 换成
`ewm(span=w, min_periods=w).std()`（adjust=True、bias=False 默认），w = 各线现役
z 窗（equal_weight 40 / citic40d 40 / hybrid20 250），分子基线 rolling mean 不动，
其余逐字节同生产语义。span=现役窗 = 等均值滞后映射，无自由参数。

| 信号线 | 分歧天数（功效） | 切换（现役→变体） | 分歧日\|r\|中位 vs 全窗（blend） | 年化容量上界（blend，无方向） |
|---|---|---|---|---|
| equal_weight | 32/3073 = **1.04%** | 133→127 | 0.99% vs 0.81%（有抬升） | 4.09%/年 |
| citic40d | 34/4040 = **0.84%** | 367→369 | 1.17% vs 0.82%（有抬升） | 1.79%/年 |
| hybrid20 | 13/3731 = **0.35%** | 178→178 | 0.32% vs 0.82% | 0.43%/年 |

**判读**：功效缺席——比 08-20 换分类器案已被判「无功效」的 9.6% 分歧天还低一个量级。
分歧日 |r| 有抬升但天数撑不起任何检验；聚集无 regime 形态（equal_weight 前 3 年仅占
50%，单年最多 6 天，连段中位 1 天）。**固定EWMA-std配置关闭；相近线性再参数化降优先级，
但未逐项实测，不能作方法族关账。**

## 3. 检测族（CUSUM）与隐状态族（Hamilton）

**共用规格**（全事前固定）：输入 = 各线自己的价差**日增量**（equal_weight 简单收益差、
citic/hybrid 日对数收益差，腿定义逐条同现役）；标准化 z_t = d_t / rolling_std(w)，
w 同上；μ0=0 不减均值（漂移正是检测对象）；聚合结构镜像现役（4 对/5 因子平均、
hybrid 主+金融确认链）；long-flat >0。

**CUSUM**：双侧反射，S±_t = max(0, S±_{t−1} ± z_t − k)，**k=0.25**（δ/2 规则，检测
0.5σ 日漂移）、**h=5.0**（教科书 ARL 值）；越限取向 ±1 并重置。
**Hamilton**：两状态高斯（均值 ±0.25、方差 1）滤波概率，参数固定不估计；对称持续
概率由现役切换时间尺度映射 p_stay = 1 − 切换次数/生效日数（ew 0.9567 / citic 0.9092 /
hybrid 0.9523）；score = 2P−1；hybrid 线的 score 过现役同一状态机（0.35/0.1/−0.15/−0.1）。

| 变体 | 分歧天数 | 切换（现役→变体） | 分歧日\|r\|中位 vs 全窗（blend） | 聚集 |
|---|---|---|---|---|
| equal_weight/CUSUM | **44.7%** | 133→**30** | 0.78% vs 0.81%（不升） | 前3年占30%≈均匀 |
| equal_weight/Hamilton | 27.4% | 133→**354** | 0.82% vs 0.81%（不升） | 均匀，连段中位2天 |
| citic40d/CUSUM | **47.4%** | 367→**48** | 0.77% vs 0.82%（不升） | 前3年占23% |
| citic40d/Hamilton | 29.5% | 367→**775** | 0.80% vs 0.82%（不升） | 前3年占21% |
| hybrid20/CUSUM | 35.5% | 178→**43** | 0.78% vs 0.82%（不升） | 前3年占27% |
| hybrid20/Hamilton | 18.7% | 178→**244** | 0.82% vs 0.82%（不升） | 前3年占29% |

（500/1000 口径方向一致，全量见附件原始输出。此分歧量级下 Σ|r| 年化上界 45%~120%/年
是平凡上界，无信息量——秤的有效部分是量级比与聚集。）

**对固定CUSUM/Hamilton规格的三条否定证据**：

1. **分歧日不是特殊日子**：六变体无一例外，分歧日 |r| 中位与全窗持平或略低。
2. **分歧不聚集在转折期**：按年均匀铺开（前 3 年占 21%~30%），2015/2024 断点年毫不
   突出——两族的立项故事（转折期反应更好）没有兑现，分歧是慢性的口径差。
3. **结构 sanity 反向翻车**：CUSUM 惰性化（每年 2~4 次切换，靠陈旧状态硬扛；救它只能
   调小 h → 走向已证伪的快信号区）；Hamilton 闪烁化（切换 2~4 倍，连段中位 2~3 天
   → 踩快腿的坟）。**这两个固定规格呈现迟钝/闪烁两难**；它支持降低同类方案优先级，
   但不等价于证明所有参数或所有因果状态估计器必然如此。现役20~40日平坦平均在当前实测中
   是更合适的证据池深度，并与多重平均降噪、参数高原两条存量结论互证。

## 4. regime 族与 probe-④ 的边界（程序合规论证）

Batch 14 探针 ④（2026-08-14，STOP）关的是「用状态变量**调制现役信号**的条件效力」：
IC 分层与 rotation 零分布不可分（p_selected 0.3836）、调节版 worst(train,val) 比现役还低
0.0126；其结果文档明确「④ 关的不是『状态无关』」。生成式 HMM 是**替换生成器**的
命题，走替换命题闸门（同秤头对头）——**程序上不是同一扇门，立项站得住**。
但本次实测表明门后是同一间空房：HMM 变体的分歧既不聚集也不落在特殊日子。
④ 的 STOP 事前削弱了「该价差有可检测 regime 结构」的先验，本档实测与其一致。

## 5. 判定与重开条件

**判定：固定EWMA-std、固定CUSUM/Hamilton与raw/DEMA插点关闭；相应方法族降优先级。**
ML未实测，仅因当前样本功效先验不足而不立项。网格外或机制不同的候选不在本次关账范围内。

重开任一族须先书面回答（缺一不开）：

1. 为何能逃出「迟钝/闪烁」两难——给出该价差日频信噪比下的检测功效论证；
2. 为何预期分歧会**聚集在转折期**——本次实测「按年均匀」即现成反例；
3. ML 族另须先解决有效样本量（~1.5e2 独立观测）对置换选优闸门的功效问题；
4. 按闸门纪律须**新预登记**；命题若为替换现役，走替换命题闸门（同秤头对头，非偏 IC）。

## 6. 附件与数据快照

- **脚本与原始输出**：`docs/plans/2026-08-26-signal-generator-prescreens/`
  （`ewma_std_prescreen.py`、`cusum_hmm_prescreen.py`、`ewma_output.txt`、
  `cusum_hmm_output.txt`）。脚本按当时运行**原样归档**（含 scratchpad 绝对 sys.path 与
  硬编码 ROOT；复现时两脚本放同一目录即可）。已知瑕疵：共用打印函数把「切换次数」
  一栏标签硬编码为「EWMA」，在 CUSUM/Hamilton 行实指该行变体——按归档保真原则不改。
  归档输出为同日同规格重跑捕获，与首跑逐字一致（分歧天数 32/34/13 与
  1374/843/1913/1192/1325/696 逐个核对）。
- **数据**：PG `stock_selector.index_daily`，评窗尾 2026-08-24 生效日；读取时刻
  2026-08-26 日更 timer（18:30）之前，库自 08-25 18:31 后无相关写入。
- **当日仓库并行事件**：另一会话在跑 `20260826T110842-p0-revalidation-95c4405`
  （五轴/P0 干净重验，SQL 只读）；本归档不触其任何文件，git 状态已于其起跑时冻入
  manifest，不受本提交影响。

## 7. 附注（同日追加）：平滑插点实测 —— raw 天花板 / DEMA(5) / DEMA(7)

登记 §2 实测的是 z **分母**插点；用户追问「5 日平滑换 DEMA 能否优化」——按
「引用旧 CLOSE 先审证据级别」的纪律，对**平滑插点**单独补测而非纯引用关门。
此插点只存在于 **equal_weight 20d40z**（citic40d / hybrid20 无平滑步）。
输入 = committed 信号 CSV（`factor_value_raw` 列即「完全不平滑」这一端），秤同上。
DEMA(n) = 2·EMA(n) − EMA(n)∘EMA(n)（adjust=False），削滞后平滑器。

| 变体 | 分歧天数（vs 现役 MA5） | 切换（现役 133→） | 因子相关 | 分歧日\|r\|中位 vs 全窗（blend） | 聚集 |
|---|---|---|---|---|---|
| raw / sm0（此插点天花板） | 285 = **9.27%** | **245** | 0.9355 | 0.89% vs 0.81% | 前3年占32%≈均匀 |
| DEMA(5) | 225 = 7.32% | 179 | 0.9686 | 0.87% vs 0.81% | 前3年占31% |
| DEMA(7) | 162 = 5.27% | 151 | 0.9832 | 0.87% vs 0.81% | 前3年占28% |

**判读三条**：

1. **任何削滞后平滑器的活动空间被 raw（sm0）这一端夹住**（DEMA 有过冲、不是严格
   内点，故逐一实算而非只引包络论证——实测确在包络内）。空间的内容物 =
   **同一批过零事件提前 2~3 天触发 + 额外抖动**——正是 2026-08-17 分批建仓探索用
   raw（本线段端点）作快腿**正面证伪**的命题（26 点测尽、5 试 5 败、机制级 STOP）。
2. **窗口参数只移动线段上的位置，改不了线段里装的东西**：5→7 分歧 7.32%→5.27%、
   相关 0.9686→0.9832，单调滑向克隆端；而分歧性质纹丝不动（|r| 抬升约 +7%、按年
   均匀、不聚转折期）。往小调滑向 raw 端 = 更多抖动，往大调滑向克隆端 = 没差别。
   **TEMA / HMA / ZLEMA 等削滞后同族，同判，不再逐个测。**
3. 切换只增不减（133→151~245），成本维为负方向；且即便有效果也只覆盖三条线之一。

**结论：平滑插点并入本登记关闭。** 重开除答 §5 三问外，须正面回应 08-17 快信号
证伪——该证伪的快腿就是本插点的天花板，覆盖全部「削滞后」方向。

附件：`dema_smoothing_probe.py` + `dema_output.txt`（本节测量的合并复现脚本与输出，
数字与当日交互首测逐位核对一致）。
