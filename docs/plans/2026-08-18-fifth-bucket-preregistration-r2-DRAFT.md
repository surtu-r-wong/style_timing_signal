# 第 5 桶 · 第二轮 预登记【草案 — 未冻结，待用户审阅】

**状态**：⚠️ **草案**。未冻结 = 未生效。**在用户明确冻结之前，不得跑 Gate 0。**
**上一轮**：`2026-08-18-fifth-bucket-preregistration.md`（`c65da56`）→ 已按其 §1 **作废**，不复活。
**根因**：`2026-08-18-replication-root-cause.md`
**用户已定（2026-08-18）**：① 两个市值带都设闸、都要过；② 今晚只做修复与开发测量，不冻结不跑闸。

---

## 0. 本轮与上一轮的关系

上一轮 Gate 0 ρ=0.7626 未过，**整轮作废**且尾部读数从未被看过——那条纪律已执行，
本轮不是"重开上一轮"，是**新的一轮**。上一轮之后落的三项代码修复（§7）改变了管线，
故必须重新过闸。

## 1. ⭐ Gate 0：**双闸，都要过**

**Gate 0A（主闸）—— 1000 带**
用与尾部桶**完全相同的代码与参数**重建 **932407 / 932406（中证1000 纯成长/纯价值）**，
与官方求**价差日收益相关 ρ**，判据 **ρ ≥ 0.85**。

**Gate 0B（底线校验）—— 2000 带**
同法重建 **932409 / 932408**，判据 **ρ ≥ 0.85**。

**OVERALL Gate 0 = 两个都过。**

### 1a. ⚠️ 两个闸的证据强度不对等，必须写明

| | Gate 0A（1000 带） | Gate 0B（2000 带） |
|---|---|---|
| 开发期是否被用作反馈信号 | **否**——从未跑过、从未看过 | **是**——2026-08-18 我拿它的 ρ 迭代过 PIT/快照/分母三处修复 |
| 证据强度 | **完整**（真·样本外） | **打折**（存在对着闸门调参的结构性风险） |
| 定位 | **主闸** | **底线校验**：防"只在 1000 带碰巧过"的偶然 |

**报告时必须原样保留这个区分，不得把 0B 的通过当成独立证据。**

### 1b. 门槛为什么仍是 0.85（不因上轮失败而改）

0.85 的出处是 B1 当年复现指数对的实测 0.88~0.91，是**先例支撑的下限**。
上一轮跑出 0.7626 之后**任何调低门槛的动作都是"调参凑闸"**。→ **维持 0.85，不改。**

### 1c. 样本空间：闸门取哪一版，现在定死

`index_constituent` 里 **000852.SH（中证1000）有 129 期真实历史成分（2014-10-31 起）**，
而尾部桶（3801+）**只能用 `total_mv` 排名带代理**（无官方指数、无成分）。

> **冻结：Gate 0A 的判据取「排名带代理」版**（`total_mv` 排名 801–1800）。
> 理由：闸门必须检验**尾部桶将要走的那条路径**；用真实成分过闸而用代理跑尾部，
> 等于闸门没测到最大的一条偏离。
> **真实成分版同时跑一遍，仅作诊断记录**（量化"代理成本 = 两版 ρ 之差"），
> **不参与判定**，且**不得**在代理版不过时改用真实成分版顶替。

### 1d. 失败后果（与上轮同）

- **任一闸不过 → 整轮作废，不得报告尾部任何读数**，只报"管线未通过复现验证"。
- **不得因不过而调参凑 ρ**；两个闸各**只跑一次**。
- 允许基于分项证据区分失败原因（分段 ρ、腿只数、单因子命中率），**不得是猜测**。

### 1e. 跑前必须先写下的东西

Gate 0A / 0B 的 ρ **必须在看尾部结果之前算出并落盘**（`backtest/output/`）。

## 2. 尾部桶定义

**沿用上一轮 §2 逐条冻结值，一字不改**（交易所 `.SH/.SZ`；`total_mv` 排名 3801+；
剔上市不足 1 年；剔 1 年 ADV 后 20%；半年度调样；成长/价值按纯风格 P 族方法论）。
上一轮从未看过尾部读数，故这些冻结值**未被数据污染**，可直接沿用。

## 3. 候选与判据

**沿用上一轮**：候选 = **5 对等权** vs 现役 **4 对等权**，**单点，不扫网格**。
（网格纪律的实测依据：1160 点网格过闸门槛 +1.27~+1.55，而整张网格最大增益仅 +0.39~+0.61，
**结构上产不出 GO**。单点候选面对单变体零分布，门槛低约 2.5~3 倍。）

关1 统计（⓪ 机器 `p_selected`）／关2 `worst(train,val)` 护栏／关3 成本与换手 —— 沿用。

## 4. 读法冻结（沿用上一轮 §5）

- 情形① 显著改善 → GO
- 情形② 不显著 → **不得**表述为"尾部无信息"，只能表述为"在现役架构下尾部的增量不可辨认"
- 情形③ 显著变差 → 记录并关帐

## 5. 已知偏离（更新版）

| # | 偏离 | 上一轮状态 | 本轮状态 |
|---|---|---|---|
| 3 | 样本空间用 `total_mv` 排名带代理官方成分 | 已登记 | **仍在**（尾部无官方成分）；1c 已把它纳入闸门检验 |
| 7 | 中信一级行业代替中证行业分类 | 已登记 | **已实测基本无害**：中证2000 里金融股仅 2 只 |
| 8 | `circ_mv` 代替自由流通市值（logistic 分位点） | 已登记 | **仍在**（`stock_indicator` 无自由流通字段） |
| 9 | 数据源 2025-03-31 切换 CSMAR→Wind | 已登记 | **仍在**，须分段核对 |
| **10** | **步骤(3) 平滑函数原文未给** → 用经验秩分位 | 已登记为自由裁量 | **仍在**；已实测**四个候选函数对成分命中率无差别**（32~33%），故不再视为主要嫌疑 |
| **11** | **PIT 可知日 = `min(ann_date, 法定披露截止日)`** | — | **本轮新增修复**。法定截止日是真实披露日的上界 → 保守非前视。⚠️ **超期披露的公司会被当成按时披露**，构成对少数标的的前视，库内无字段可剔 → 结论仍标 `provisional` |
| **12** | **`stock_financial` 停更在 2025-03-31** | — | **未修**（上游缺口）。→ 2026 年的 asof 用 15 个月前财务；**本轮所有结论的有效窗口截至 2025-03-31**，其后为数据外推 |

## 6. 明确的风险与预期

| 风险 | 处置 |
|---|---|
| 修了三处仍过不了 0.85 | 整轮作废；此时应认真考虑"中证纯风格指数在公开信息下不可复刻"这一可能，转向自建分类器路线（但会失去 Gate 0 的验证价值） |
| 只过 0A 不过 0B（或反之） | 按 §1 = 未通过。**不得**只报过的那个 |
| 过了闸但尾部候选是情形② | 按 §4 措辞，不得升格为"尾部无信息" |

## 7. 本轮之前已落的代码修复（跑闸前须已在树上）

1. **PIT 可知日修正**（`_fetch_series`）—— 每个调样日找回最近整一个季度
2. **快照护栏**（`sample_space` 取窗口峰值一半以上行数的最近交易日）+ **空样本空间出声告警**
   （上一轮因此静默少建一期，全程零告警）
3. **`avg_mv_1y` 改日频**（`close × ffill(total_shares)`，与库内 `total_mv` 实测完全一致）

## 8. 本轮新增纪律

- **空结果必须出声**：任何"跳过/为空/全 NaN"的分支都要打印并计入返回值。
  本项目已两次被静默失败咬到（SalG/ProG 全空；整期被跳过）。
- **跑全窗之前先做单期人工核对**：至少一个调样日，逐项打印非空率与腿只数。
