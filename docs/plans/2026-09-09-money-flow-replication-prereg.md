# 资金流向面 F1·横截面复制预登记（2026-09-09，**待用户冻结；未看任何目标收益读数**）

> 仓里第二份复制预登记（第一份 `2026-09-09-basis-term-replication-prereg.md`，已收官 STOP）。
> 缘起同前：用户否定「重开 = 等 2026-09 之后新增数据」。盘点见 `2026-09-09-cross-section-replication-inventory.md`。

## 0. 立项与线索

原线 `2026-09-03-money-flow-axis-results.md`：F1（沪深300 + 创业板综两腿主力残差净流入均值，慢腿化）
代表 `F1_lb5zw250` / k=20 / 方向 +1，全窗 IC 0.2599、偏 IC 0.2779、两半窗 0.344/0.263、净 Sharpe 1.02，三关全过，
只败关 0（max-T 门槛 0.337，该档功效 0.078）。原线预登记 §2 把**科创板腿 000680.SH 明文降为「稳健性腿，PASS 后事后用」**——
它的残差序列 `e_000680.SH` 在缓存里早已算好，**从未进过任何网格、任何 IC**。本预登记把它升为独立复制对象。

**冻结前已看过的东西（如实披露，全部不含目标收益）**：
- 盘点子代理的库覆盖统计（`index_money_flow` 三板块起止、字段清单）。
- `e_000680.SH` 的描述统计、逐年均值、有效起止（2020-07-06..2026-09-03，1,498 日；beta 预热 120 日在前）。
- **独立性预检 I0**（§2.4）：科创腿残差与 F1 及其两条成腿的相关，原始层与信号层。
- 未看：科创腿任何形态对任何目标的 IC、p、Sharpe；F1 对 300/2000/全指的任何读数。

**先验**：科创腿与 F1 信号层相关 0.289，是三条复制线里最独立的输入；但只覆盖 2021-07 之后（原线后半窗），
2015-2020 段无对应物。基差线的教训：输入侧相关不预告可迁移性，I0 只用来判「是否同读数」。

## 1. 命题（方向已冻结）

「市场级主力残差净流入（剔同日涨跌）经 5 日均值 / 250 日 z / tanh 慢腿化后，对 500/1000 blend 未来 20 日收益有正向预测力」
这一在 300+创业板两腿上观察到的形态，在**科创板单腿**上是否复现。目标不变（blend），换的是输入宇宙——
`marketmoneyflows` 白名单只有三板块，科创板是唯一没碰过的那个。

## 2. 构造（冻结，逐字继承原线）

- 数据：`stock_selector.index_money_flow` 000680.SH 的 `main_in_money / main_out_money`；`f=(in−out)/(in+out)`；
  `e_t = f_t − a_{t−1} − b_{t−1}·r_t`，(a,b) 为 [t−250, t−1] OLS（min 120），r 取 `index_daily` 000680.SH 收益（`money_flow_series.residualize_pit`）。
- 信号：`level_signal(e, lb=5, zw=250)`；k=20 非重叠窗；**方向 +1**。单腿不做均值（原线 F1 是两腿均值；科创只有一腿）。
- **评窗起点 = 首个 zw 满窗日 2021-07-13**（原线把 250 日 burn-in 的 0 值一并计入评窗；本线剔除，只会更干净，不改变构造）。
- 机器：序列复用 `backtest/money_flow_series.py` 缓存列 `e_000680.SH`；探针 `backtest/money_flow_replication.py`
  （复用 `basis_term_replication.one_sided_shift_pvalue`），单测 + 变异验证见提交。
- PIT：资金流与收盘同日可知，T+1 生效（同原线）。

### 2.4 独立性预检 I0（已算，判据同基差线：信号层相关 > 0.8 → 同读数）

| 层 | corr(科创腿, F1) | corr(科创腿, e_300) | corr(科创腿, e_创业板) | 重叠 |
|---|---|---|---|---|
| 原始残差 | 0.281 | 0.229 | 0.267 | 1,496 日 |
| 5 日均值 | 0.355 | — | — | |
| **信号层 lb5zw250（满窗后）** | **0.289**（Spearman 0.273） | — | — | 1,247 日 |

判定：**独立读数**。

## 3. 检验、窗口与判定（冻结）

| # | 输入 | 目标 | 窗口 | k=20 窗数 | 检验 |
|---|---|---|---|---|---|
| P1 | 科创腿 e 信号 lb5zw250 | blend（500/1000 现货等权） | 2021-07-13..跑当日 | 62 | 非重叠 Spearman IC；循环移位置换 1,000 次，**单侧** p（方向 +1） |

无网格、无选优 → 关 0 不适用，单侧置换 p 即正式口径。
两半窗按可用样本中点 **2021-07-13..2024-02-01 / 2024-02-02..** 只报不闸。

**判定**：PASS = P1 单侧 p < 0.05；否则 STOP，**终局**。PASS 后不进生产，下一步是与原线 F1 同秤合并的装配设计（两腿 + 科创腿三腿均值是否等价于原构造须另论证）。

### 3.3 描述性副产物（只报不闸）

- P1 的偏 IC（控现役 equal_weight）与 p；净 Sharpe 不算（原线关 3 已过，本线只问机制）。
- 科创腿信号 → 000300.SH / 932000.CSI / 000985.CSI 的 IC（目标侧辅助）。
- **原线冻结的 F1 信号**（`F1_lb5zw250`）→ 000300 / 932000 / 000985（2015-07 起，约 135 窗）：目标侧复制，只报；
  它与 blend 共享市场 beta，不作证据。
- 逐年 IC 符号。

## 4. 功效边界与引用纪律

- n=62：真 ρ=0.26（原线代表）单侧 α=0.05 功效约 0.64；ρ=0.20 约 0.46。**低于 0.5 的部分是本线的固有限制**，STOP 时只能说「未复现」不能说「无效应」。
- 不得引作：「资金流对 blend 无信息」（原线已 STOP 并带限定）；「科创板资金流无信息」（只测一个冻结规格）。
- 不登记的形态：其它 lb/zw/k、方向 −1、F2 型（科创 − 300）、同表四档/开尾盘字段——要测须另立预登记。

## 5. 数据前置（已实测，只读）

| # | 闸 | 判据 | 实测 |
|---|---|---|---|
| 1 | 残差有效段 | 自 beta 预热后无缺日 | 2020-07-06..2026-09-03 共 1,498 日；2020-01-02 起 120 个 NaN 全为预热 |
| 2 | 比率量纲 | f ∈ [−1, 1] | 实测 [−0.23, 0.156] |
| 3 | 残差量级 | 与两成腿同量级 | std 0.0254，逐年均值 ±0.007 内 |
| 4 | 目标覆盖 | blend 覆盖评窗 | 000905/000852 至 2026-09-03 |

## 6. 输出

`backtest/output/money_flow_replication_{panel.csv,verdict.json}`；结果文档 `2026-09-XX-money-flow-replication-results.md`；登记表 id `money-flow-cross-section-replication`。
