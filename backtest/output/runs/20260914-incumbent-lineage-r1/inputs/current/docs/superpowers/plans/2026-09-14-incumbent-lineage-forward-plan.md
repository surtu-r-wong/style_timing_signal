# 现役参数历史与前瞻观察准备（2026-09-14）

用户授权：继续补查现役选择历史与未使用样本的确认条件。

## 完成范围

1. 按Git提交追溯equal_weight 20/40/5、四对配置、slope L20/zw120/sm0及两池映射；从历史CSV重放旧/新代表规则，不重优化现役。
2. 冻结截至本轮已用历史的边界；为未来观察固定参数、比较对象、成交时点、费用、资金池及观察次数。统计确认仍须匹配该统计量的独立校准，不能沿用已撤回保证。
3. 新增只读生产信号、只追加本地证据的记录器；严格禁止跨日补签或使用带未来日期的信号。保留原文件字节、代码/协议指纹和记录链。建立零观察的初始campaign，不安装定时任务，不改变生产输出。
4. 输出历史追溯报告、协议及可核验run，更新研究登记。

## 文件与行为

- `backtest/forward_observation.py`：init / record / verify；CLI使用系统当前UTC时间，转Asia/Shanghai。仅同一自然日16:00后接受当日完整两信号，且日期不早于协议首个信号日。
- `tests/test_forward_observation.py`：及时录入、拒绝旧/未来日期、拒绝盘中、拒绝协议或代码漂移、拒绝重复、篡改检出。
- 协议固定最早信号日2026-09-15，首笔成交为该信号下一实际交易日收盘。9月14日及以前只作已用历史。没有合格及时记录的日子必须标记缺口，不能事后补成前瞻。
- 记录器不读取账户流水、不下单、不重算收益或生成显著性裁决。输入行情的历史可得性仍需来源记录；本机哈希和时间戳不是外部公证。

## 验证

先跑行为测试确认新模块缺失，再实现并跑同一组测试；研究登记检查、生产文件哈希和不可变run清单校验。对无变化的既有数值模块复用前轮验证。
